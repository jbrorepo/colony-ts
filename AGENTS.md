# The Colony - Agent Operating System (TypeScript/Bun)

## What This Project Is

The Colony is a security-first, local-first AI agent platform with a caste-based
runtime identity model, conservative approvals, exact transcript preservation,
and Caveman token-savings for model-bound and internal agent traffic.

Antelligence is the canonical launch-facing operating-model framework for
explaining how The Colony applies those principles to enterprise product
development. Treat it as product philosophy and adoption guidance, not as proof
that deferred runtime surfaces are shipped.

## Repository Layout

Primary directories:

- `src/runtime/` - `AgentLoop`, approvals, prompt assembly, compaction, tools
- `src/llm/` - providers, failover, rate limiting, cost tracking, Caveman bridge
- `src/ui/` - React/Ink operator interface
- `src/memory/` - transcript logging, recall, fact extraction
- `src/mempalace/` - async-safe MemPalace foundation, hierarchy still in progress
- `src/security/` - vault, path validation, bash validation, audit, log sanitization
- `docs/` - roadmap, execution plan, gap analysis, benchmark board, references

## Current State

- Real `AgentLoop` is wired to the Ink UI with anti-flicker streaming.
- Providers: Ollama, Anthropic, OpenAI-compatible, Gemini.
- Compaction, approvals, provider health, startup doctor, persisted session recovery, and memory handoff are live.
- Verification gate is `verify:all`, currently through `verify:phase282` plus `tsc --noEmit`; Launch Alpha 0 also has the focused `verify:alpha0` gate.
- The 12-caste method framework is compatibility-first: legacy Python caste values remain accepted for persisted runtime state, while public/operator language prefers Queen, Eldest, Assist-Ant, Command-ant, Vigil-ant, Develop-ant, Logist-ant, Consult-ant, Inform-ant, Cogniz-ant, Account-ant, and Oper-ant.
- Phase 112 ships signed Discord `APPLICATION_COMMAND` inbound dispatch inside the host-owned external webhook transport: verified command interactions still require injected vendor signature verification plus host webhook/inbound auth, normalize into the existing channel/session bridge, and return a bounded Discord deferred ACK `{ "type": 5 }` after successful local bridge acceptance, without emitting an unmatched immediate adapter reply. It still creates no Discord apps, persists no credentials or interaction tokens, starts no default listeners, provides no public hosting, uploads no media, enables no privileged Gateway intents, and enables no default live inbound delivery.
- Phase 113 ships Slack setup scope preflight and activation-readiness UX: approved host-owned Slack setup now checks the manifest has the required `channels:history` bot scope for `message.channels` before fetch, and successful manifest mutation reports success-only readiness plus remaining operator steps without claiming live delivery, public hosting, credential persistence, listener startup, uploads/media, or retry workers.
- Phase 114 ships Slack `event_callback` fast ACK inside the host-owned external webhook transport: signed and host-authenticated Slack message events are accepted into the channel/session bridge and return 202 before runner/reply completion, while asynchronous adapter replies still preserve Slack thread routing. It still creates no Slack apps, persists no credentials or tokens, starts no default listeners, provides no public hosting, uploads no media, runs no retry worker, and enables no default live inbound delivery.
- Phase 115 ships Slack `app_mention` as an approval-bound alternative Events API subscription and inbound event shape: mention-only candidates require `app_mentions:read`, patch only `app_mention` bot events, and signed/host-authenticated mention callbacks use the existing deferred ACK bridge path. It still does not mix Slack event subscriptions, create Slack apps, persist credentials or tokens, start default listeners, provide public hosting, upload media, run retry workers, or enable default live inbound delivery.
- Phase 116 ships bounded in-memory duplicate inbound message-id suppression at the channel session bridge route boundary, verified for signed/host-authenticated Slack `message` and `app_mention` event retries. Duplicate callbacks receive a bounded 202 ACK with `turnStatus: "duplicate"` and no second runner dispatch, route message-count increment, or adapter reply. It still persists no dedupe state, credentials, tokens, or approval signatures; starts no default listeners; provides no public hosting; uploads no media; runs no retry worker; and enables no default live inbound delivery.
- Phase 117 ships a Slack approved event binding gate: approved host-owned Slack setup can append redacted durable event binding records, and signed/host-authenticated Slack callbacks can be fail-closed before bridge acceptance unless a supplied binding reader/list matches the workspace and event type. It persists only redacted refs/fingerprints, not tokens, signing secrets, manifests, approval signatures, or callback URLs; it still starts no default listeners, provides no public hosting, uploads no media, runs no retry worker, and enables no default live inbound delivery.
- Phase 118 ships inbound-only Slack media metadata inspection: signed and host-authenticated Slack `file_share` callbacks can enter the existing deferred ACK/session bridge path with bounded, redacted file metadata, and Slack `app_mention` callbacks with files preserve the same safe metadata. It does not fetch, download, upload, preview, persist private file URLs, start listeners, provide public hosting, run retry workers, or enable default live inbound delivery.
- Phase 119 through Phase 225 ships the host-owned external media transfer/manual-reinvoke chain from approval-bound handoff through the retry-control worker invocation execution receipt closeout, closeout-record, work-item closure, audit-record planning, supplied audit-record-plan preflight, append-only redacted retry-control work-item closure audit-record persistence, trusted supplied audit-record persistence preflight, trusted preflight-bound retry-ledger entry planning, append-only redacted retry-control work-item closure retry-ledger entry persistence, trusted supplied retry-control closure retry-ledger entry preflight, trusted preflight-bound retry-ledger entry readiness planning, supplied readiness-plan preflight, trusted readiness-plan-preflight-bound operator handoff, supplied operator-handoff preflight, closure retry-ledger entry worker-selection intent, trusted host-supplied worker handler-readiness, trusted closure retry-ledger entry worker invocation-handoff truth, supplied invocation-handoff preflight truth, trusted preflight-bound worker execution-plan truth, supplied execution-plan preflight truth, trusted execution-plan-preflight-bound foreground worker execution, deterministic redacted non-persistent worker execution receipt truth, trusted supplied worker execution-receipt preflight truth, trusted receipt-preflight-bound worker execution-receipt closeout readiness truth, trusted supplied worker execution-receipt closeout preflight truth, and trusted closeout-preflight-bound worker execution-receipt closeout record-plan truth. Phase 225 accepts only trusted supplied Phase224 closeout preflights after recomputing current execution-receipt closeout preflight truth, rejects copied, tampered, missing, contaminated, or nested-mutation attempts, and still creates no Colony retry worker, retry schedule, credential persistence, public hosting, default live delivery, background retry, automatic vendor retry, receipt persistence, default listener, closeout persistence, durable closeout record, or raw host-data persistence. These phases preserve host-owned execution and do not create default Colony fetch/download/upload workers, extend text adapters with attachments, persist credentials/approval signatures/file bytes/private URLs/raw target IDs, start listeners, provide public hosting, run background retry workers, retry vendor sends after an ambiguous send attempt, verify late vendor delivery, or enable default live delivery.
- Phase 126 ships redacted external subscription credential setup readiness UX for Slack and Discord: `/channels external` now renders required credential-ref labels, host-supplied runtime secret/config responsibilities, missing/invalid ref status, and no-persistence/no-default-live-delivery guardrails for subscription setup plans; accepted subscribe staging also names the host-only credential handoff. It stores no credential values or raw refs and does not mutate vendors, start listeners, provide public hosting, or enable default live delivery.
- `/swarm` is a real planner/worker/reviewer coordinator path with coordinator-only and LLM-backed execution modes, detailed status, resume, retry, cancel behavior, and durable restart-safe run snapshots when configured with `JsonSwarmRunStore`.
- GitHub local execution has deterministic provided issue-reference/URL intake feeding the approval-gated branch/worktree action plan plus verified PR handoff artifacts; it still does not fetch GitHub, push, or create PRs by default.
- Web control has a local-only authenticated operator shell with provider/workflow/swarm/channel status and host-mediated local action controls; it still does not provide hosted/public delivery or direct mutation execution.
- Phase 232 adds real-session memory/MemPalace recall QA coverage for exact transcript truth, derived ownership/decision facts, palace path routing, procedure routing, section ordering, and body-safe diagnostics.
- Phase 233 adds swarm stage timeline fields and `/swarm status` rendering for started/ended times, attempt counts, token/cost metadata, artifact counts, redacted artifact review previews, latest failures, retry history, injected failure coverage, restart-safe cancellation preservation, interrupted-stage resume history, and persisted approval-wait preservation across resume/retry.
- Phase 234 starts the Beta 2 MCP/plugin execution fabric by adding approval-gated plugin package install/update execution receipts through an injected executor. It requires exact plugin trust approval, constrains package paths under the approved root, blocks lifecycle scripts unless `--ignore-scripts` is present, records redacted stdout/stderr previews, stops after failed commands, and still does not fetch registries, activate live plugin records, start sidecars, or persist credentials.
- Phase 235 adds supplied registry metadata enrichment to plugin package planning: matching host-supplied metadata records checksum/integrity/signature summaries, rejects identity/source/digest mismatches before sidecar trust signatures are produced, redacts raw signatures and unsafe registry URLs, and still performs no live registry fetch, activation, sidecar startup, package execution, or credential persistence.
- Phase 236 adds approval-gated plugin package sidecar activation controls: activation requires an import/update action, matching completed install/update receipt, exact plugin trust approval, and an injected sidecar supervisor start. Receipts redact package sources, approval reasons, supervisor lifecycle details, and transport/client internals; live registry fetch, default activation, package lifecycle execution, transport creation, and credential persistence remain disabled.
- Phase 237 adds the explicit plugin registry host/network boundary: registry metadata fetch can now be represented as a high-risk approval-gated host handoff with HTTPS/no-credential/no-local URL validation, exact signatures, redaction, and no Colony-owned network execution. It still performs no live registry fetch, package execution, activation, catalog mutation, or credential persistence.
- Phase 238 adds the approval-gated plugin registry fetch execution receipt: a ready Phase 237 handoff plus exact approval can call only an injected host executor, validate returned JSON metadata identity/digest/content-type/size, and persist only a redacted receipt. It still creates no built-in Colony registry client, default marketplace fetch, package execution, activation, catalog mutation, or credential persistence.
- Phase 239 adds plugin package code execution policy preflights: exact approval plus a matching completed install/update receipt can produce a redacted host-action preflight for bounded test/build/lint package commands, while lifecycle scripts, shell-like commands, path escapes, signature tampering, unknown sidecars, and receipt mismatches fail closed. It still executes no plugin package code, calls no executor, starts no sidecar, fetches no registry, mutates no catalog, and persists no credentials.
- Phase 240 adds approved plugin package code execution receipts: a ready Phase 239 preflight plus exact approval can call only an injected host executor once, records redacted bounded stdout/stderr and exit code truth, fails closed on tampered preflights/path escapes/missing approval, and still fetches no registry, activates no sidecar, mutates no catalog, and persists no credentials.
- Phase 241 adds a read-only built-in plugin marketplace view over bundled package descriptors: it plans safe import/update/keep/reject actions, hashes queries without echoing raw text, redacts package sources and unsafe metadata, and still fetches no registry, installs no packages, executes no package code, activates no sidecar, mutates no catalog, and persists no credentials.
- Phase 242 adds read-only plugin marketplace activation readiness: it summarizes whether bundled descriptors need review, install/update receipts, exact activation approval, or an explicit operator handoff, while still fetching no registry, installing no packages, executing no package code, activating no sidecar, mutating no catalog, and persisting no credentials.
- Phase 243 adds the redacted plugin marketplace activation handoff descriptor: ready activation-readiness entries can produce an approval-bound host-action descriptor for the injected supervisor activation path, while still performing no activation, sidecar start, registry fetch, package install, package execution, catalog mutation, or credential persistence.
- Phase 244 adds approved plugin marketplace activation handoff execution receipts: a ready Phase 243 handoff plus matching action, completed install/update receipt, exact approval, and injected supervisor can delegate to the existing sidecar activation helper, while still performing no registry fetch, package install, package-code execution, catalog mutation, or credential persistence.
- Phase 245 adds a read-only plugin marketplace activation execution status view: readiness entries can be projected against approved handoff-execution receipts and active sidecar signatures to show `not_executed`, `blocked`, `failed`, `completed`, or `active` operator state without starting sidecars, fetching registries, installing packages, executing package code, mutating catalogs, or persisting credentials.
- Phase 246 adds a redacted plugin marketplace install/update handoff descriptor: bundled marketplace entries that require import/update can produce an exact-approval host-action descriptor for the existing injected install/update executor path, while still performing no registry fetch, package install, package-code execution, sidecar activation, sidecar start, catalog mutation, or credential persistence.
- Phase 247 adds a read-only plugin marketplace install/update execution status view: handoff descriptors can be projected against supplied install/update receipts to show `not_executed`, `blocked`, `failed`, or `completed` operator state without fetching registries, installing packages, executing package code, activating sidecars, starting sidecars, mutating catalogs, or persisting credentials.
- Phase 248 adds an approved plugin marketplace install/update handoff execution bridge: ready handoffs delegate only to `executeApprovedPluginPackageInstallUpdate` after exact handoff/action/approval validation, with failed/blocked receipt truth, redaction, and no registry fetch, package-code execution, sidecar activation, sidecar start, catalog mutation, or credential persistence.
- Phase 249 adds marketplace registry metadata fetch handoff descriptors over bundled marketplace entries: ready handoffs delegate only to the existing `executeApprovedPluginPackageRegistryFetch` host-executor path after exact registry approval validation, embed the generic Phase 237 registry boundary truth, block missing entries, missing/unsafe registry URLs, wrong approvals, and rejected package actions, and still perform no network fetch, install/update, package-code execution, sidecar activation/start, catalog mutation, or credential persistence.
- Phase 250 adds a read-only marketplace registry fetch execution status view: registry fetch handoffs can be projected against supplied registry-fetch receipts to show `not_executed`, `blocked`, `failed`, or `completed` operator state without fetching registries, installing/updating packages, executing package code, activating/starting sidecars, mutating catalogs, or persisting credentials.
- Phase 251 adds approved marketplace registry fetch handoff execution receipts: ready handoffs delegate only to the existing injected `executeApprovedPluginPackageRegistryFetch` helper after matching handoff/action/approval validation, blocked or tampered handoffs fail before executor calls, failed host-executor receipts are summarized, and Colony still creates no built-in registry client, package install/update, package-code execution, sidecar activation/start, catalog mutation, or credential persistence.
- Phase 252 adds read-only marketplace registry-fetch metadata planning: completed registry-fetch receipts can gate host-supplied full registry metadata before it is passed to the existing package planner, while redacted receipts alone remain insufficient trust input. It still performs no registry fetch, install/update, package-code execution, sidecar activation/start, catalog mutation, or credential persistence.
- Phase 253 adds metadata-bound marketplace install/update handoff descriptors: a Phase 252 `metadata_ready` planning entry is required before the existing marketplace install/update handoff can become usable for that bundled entry. It still performs no registry fetch, install/update execution, package-code execution, sidecar activation/start, catalog mutation, or credential persistence.
- Phase 254 adds approved metadata-bound marketplace install/update handoff execution receipts: a ready Phase 253 metadata-bound handoff must pass its metadata gate before delegating to the existing approved marketplace install/update execution helper. It still performs no registry fetch, package-code execution, sidecar activation/start, catalog mutation, or credential persistence.
- Phase 255 adds read-only metadata-bound marketplace install/update execution status: Phase 253 handoffs can be projected against Phase 254 receipts into metadata-blocked/not-executed/blocked/failed/completed states without registry fetch, install/update execution, package-code execution, sidecar activation/start, catalog mutation, or credential persistence.
- Phase 256 adds read-only metadata-bound marketplace activation readiness: metadata-bound install/update execution status can be projected into metadata-blocked/install-not-executed/install-blocked/install-failed/needs-activation-approval/ready-for-activation-handoff/active states without registry fetch, install/update execution, package-code execution, sidecar activation/start, catalog mutation, or credential persistence.
- Phase 257 adds redacted metadata-bound marketplace activation handoff descriptors: ready metadata-bound activation-readiness entries can produce an approval-bound host-action descriptor for the injected supervisor handoff path without registry fetch, install/update execution, package-code execution, sidecar activation/start, catalog mutation, or credential persistence.
- Phase 258 adds approved metadata-bound marketplace activation handoff execution receipts: ready Phase 257 handoffs can delegate to the existing approved marketplace activation execution helper only after matching handoff/action/install-receipt/approval validation, while blocked/tampered gates fail before supervisor calls and it still performs no registry fetch, install/update execution, package-code execution, catalog mutation, or credential persistence.
- Phase 259 adds read-only metadata-bound marketplace activation execution status: Phase 256 readiness entries can be projected against Phase 258 receipts and active sidecar signatures into metadata-blocked, activation-not-ready, not-executed, blocked, failed, completed, or active operator states without registry fetch, install/update execution, package-code execution, sidecar start, catalog mutation, or credential persistence.
- Phase 260 adds read-only plugin marketplace lifecycle status: existing metadata planning, metadata-bound install/update execution status, metadata-bound activation readiness, and metadata-bound activation execution status can be projected into a single operator board with metadata/install/activation/completed/active states without registry fetch, install/update execution, package-code execution, sidecar start, catalog mutation, or credential persistence.
- Phase 261 adds read-only plugin marketplace lifecycle handoff: lifecycle-board states can be projected into bounded operator-only checklist actions with approval-required versus inspect-only summaries and optional state/cap filtering, while still performing no registry fetch, install/update execution, package-code execution, activation, sidecar start, catalog mutation, or credential persistence.
- Phase 262 adds read-only plugin marketplace lifecycle runbook: lifecycle handoffs can be ordered into bounded operator/reviewer runbook steps with approval-focused filtering and cap controls, while still performing no registry fetch, install/update execution, package-code execution, activation, sidecar start, catalog mutation, or credential persistence.
- Phase 263 adds read-only plugin marketplace lifecycle approval packets: approval-required lifecycle runbook steps can be projected into bounded operator/reviewer approval packets with metadata/install/update/activation subject labels, prerequisite-aware ordering, phase/cap filtering, redaction, and no registry fetch, install/update execution, package-code execution, activation, sidecar start, catalog mutation, or credential persistence.
- Phase 264 adds read-only plugin marketplace lifecycle approval review: lifecycle approval packets can be checked against supplied approval evidence into ready/missing/mismatch/expired operator states with state/cap filtering, signature redaction, host-action denial, and no registry fetch, install/update execution, package-code execution, activation, sidecar start, catalog mutation, or credential persistence.
- Phase 265 adds read-only plugin marketplace lifecycle approval handoff: reviewed lifecycle approval packets can be projected into operator-only handoff descriptors with ready/missing/mismatch/expired states, phase/action/cap filtering, redaction, host-action denial, and no registry fetch, install/update execution, package-code execution, activation, sidecar start, catalog mutation, or credential persistence.
- Phase 266 adds read-only plugin marketplace lifecycle approval handoff preflight: lifecycle approval handoffs can be projected into bounded host-handoff preflight descriptors with ready/blocker states, next-action guidance, host-handoff metadata redaction, state/cap filtering, host-action denial, and no registry fetch, install/update execution, package-code execution, activation, sidecar start, catalog mutation, or credential persistence.
- Phase 267 adds read-only plugin marketplace lifecycle host handoff request descriptors: approval handoff preflights can be projected into bounded operator/host request packets with explicit request commands, descriptor-only payloads, state/kind/cap filtering, blocker preservation, redaction, host-action denial, and no registry fetch, install/update execution, package-code execution, activation, sidecar start, catalog mutation, or credential persistence.
- Phase 268 adds read-only plugin marketplace lifecycle operator queues: host handoff request descriptors can be grouped into approval-blocker, metadata, install/update, activation-readiness, and activation request lanes with operator actions, state/lane/cap filtering, redaction, default-live-execution denial, and no registry fetch, install/update execution, package-code execution, activation, sidecar start, catalog mutation, or credential persistence.
- Phase 269 adds read-only plugin marketplace lifecycle default UX plans: operator queues can be projected into default prompt descriptors for metadata, install/update, activation-readiness, activation, approval collection, and approval refresh actions with command labels/ids, blocker preservation, redaction, default-live-execution denial, and no registry fetch, install/update execution, package-code execution, activation, sidecar start, catalog mutation, or credential persistence.
- Phase 270 adds read-only plugin marketplace lifecycle default UX command palettes: default UX plans can be projected into operator command-palette entries grouped as host handoff requests and approval remediation with stable command previews, safety badges, blocker preservation, redaction, default-live-execution denial, and no registry fetch, install/update execution, package-code execution, activation, sidecar start, catalog mutation, or credential persistence.
- Phase 271 adds read-only plugin marketplace lifecycle default UX command briefs: command palettes can be projected into copy-safe operator brief lines grouped as ready commands and approval remediation with stable display text, request-bound copy text, blocker preservation, redaction, default-live-execution denial, and no registry fetch, install/update execution, package-code execution, activation, sidecar start, catalog mutation, or credential persistence.
- Phase 272 adds read-only plugin marketplace lifecycle default UX command transcripts: command briefs can be projected into bounded terminal transcript lines and fixed safety summaries with operator-only hints, blocker preservation, redaction, default-live-execution denial, and no registry fetch, install/update execution, package-code execution, activation, sidecar start, catalog mutation, or credential persistence.
- Phase 273 adds read-only plugin marketplace lifecycle default UX command panels: command transcripts can be projected into operator panel cards grouped as ready commands, approval remediation, and safety summaries with copy text, terminal hints, blocker preservation, redaction, default-live-execution denial, and no registry fetch, install/update execution, package-code execution, activation, sidecar start, catalog mutation, or credential persistence.
- Phase 274 adds read-only plugin marketplace lifecycle default UX command panel digests: command panels can be projected into concise operator digest items grouped as ready commands, approval remediation, and safety summaries with copy text, terminal hints, blocker preservation, redaction, default-live-execution denial, and no registry fetch, install/update execution, package-code execution, activation, sidecar start, catalog mutation, or credential persistence.
- Phase 275 adds read-only plugin marketplace lifecycle default UX command panel digest clipboard packets: command panel digests can be projected into bounded operator clipboard packets with ready-command/remediation sections, safety summaries, copy text, clipboard text, blocker preservation, redaction, default-live-execution denial, and no registry fetch, install/update execution, package-code execution, activation, sidecar start, catalog mutation, or credential persistence.
- Phase 276 adds read-only plugin marketplace lifecycle default UX command panel digest clipboard review packets: clipboard packets can be projected into bounded operator/reviewer review checklists with ready-command/remediation sections, safety summaries, review text, review-ready/blocker states, blocker preservation, redaction, default-live-execution denial, and no registry fetch, install/update execution, package-code execution, activation, sidecar start, catalog mutation, or credential persistence.
- Phase 277 adds read-only plugin marketplace lifecycle default UX command panel digest clipboard review closeout packets: review packets can be projected into bounded operator/reviewer closeout checklists with ready-command/remediation sections, safety summaries, closeout text, closeout-ready/blocker states, blocker preservation, redaction, default-live-execution denial, and no registry fetch, install/update execution, package-code execution, activation, sidecar start, catalog mutation, or credential persistence.
- Phase 278 adds read-only plugin marketplace lifecycle default UX command panel digest clipboard review closeout summary packets: closeout packets can be projected into bounded operator/reviewer summary checklists with ready-command/remediation sections, safety summaries, summary text, summary-ready/blocker states, closeout/review-line preservation, blocker preservation, redaction, default-live-execution denial, and no registry fetch, install/update execution, package-code execution, activation, sidecar start, catalog mutation, or credential persistence.

## Critical Rules

1. All security logic stays pure TypeScript with zero npm dependencies.
2. All LLM providers use raw `fetch()`.
3. Default tool approval remains conservative unless intentionally relaxed.
4. Never add blocking I/O to the main event loop.
5. Tool results over 10 KB must be externalized and redacted before persistence.
6. Keep canonical transcript truth verbatim and separate from derived compact artifacts.
7. React/Ink streaming remains anti-flicker: buffer in refs, flush on a bounded cadence.
8. Install the log sanitizer before bootstrapping UI/runtime modules that may emit secrets.

## Reference Docs

Read these before large changes:

- `docs/COLONY_BIBLE.md`
- `docs/DECISIONS.md`
- `docs/ROADMAP.md`
- `docs/EXECUTION_PLAN.md`
- `docs/GAP_ANALYSIS.md`
- `docs/BENCHMARK_BOARD.md`
- `docs/REFERENCES.md`
- `docs/ANTELLIGENCE_PLAYBOOK.md`
- `docs/ANTELLIGENCE_LAUNCH_CHECKLIST.md`

## Porting Rule

When porting a Python behavior:

1. Read the Python source first.
2. Preserve behavior and failure semantics.
3. Adapt to TypeScript/Bun idioms.
4. Add or extend a verification phase script.

## What Is Next

Current implementation order:

1. Phase 2 - memory polish only when concrete retrieval precision or inspection gaps are discovered
2. Release-grade Alpha 0 hardening around source+Bun onboarding, claim safety, build packaging, and the real LLM swarm demo.
3. Richer swarm visualization and production hardening beyond the Launch Alpha 0 real planner/worker/reviewer execution path
4. Remote action controls and workflow automation hardening beyond the daemon/client/control-plane foundation
5. Phase 2 / Alpha 3 memory polish only when new concrete recall, inspection, or recovery gaps are discovered
6. Phase 7 - end-to-end hardening and release gates
