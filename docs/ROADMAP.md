# The Colony - Forward Roadmap

Last Updated: 2026-05-14

This roadmap replaces old milestone text that assumed the UI was still stubbed. That is no longer true. The Colony now has a working single-agent spine. Remaining work is product completion, memory, workflow, multi-agent coordination, gateway breadth, and release hardening.

For current built/not-built truth, see [PROJECT_STATE.md](./PROJECT_STATE.md). For full task detail, see [EXECUTION_PLAN.md](./EXECUTION_PLAN.md). For subsystem parity status, see [GAP_ANALYSIS.md](./GAP_ANALYSIS.md). For capability-class parity vs Claude Code and OpenClaw, see [BENCHMARK_BOARD.md](./BENCHMARK_BOARD.md). For the latest full competitor gap analysis, see [release/COMPETITOR_GAP_ANALYSIS_2026-05-11.md](./release/COMPETITOR_GAP_ANALYSIS_2026-05-11.md). For closure sequencing across independent benchmark tracks, see [PARITY_EXECUTION_BOARD.md](./PARITY_EXECUTION_BOARD.md). For launch-facing operating-model language, see [ANTELLIGENCE_PLAYBOOK.md](./ANTELLIGENCE_PLAYBOOK.md). For the public source+Bun alpha scope, see [LAUNCH_ALPHA_0.md](./LAUNCH_ALPHA_0.md).

## Current Baseline

- Real `AgentLoop` wired to Ink UI with streaming output.
- Provider stack present: Ollama, Anthropic, OpenAI-compatible, Gemini.
- Failover, usage tracking, rate limiting, effort selection, and per-model cost drill-down present.
- Conservative per-call approvals present.
- Standard and reactive compaction present.
- Structured runtime logging present.
- Tool result externalization present.
- Prompt identity and prompt assembly present, with the 12-caste method framework
  preferred in operator/model-facing display while legacy Python caste values
  remain accepted as compatibility aliases for persisted runtime state.
- Persisted single-agent session resume foundation present.
- Persisted session catalog is exposed in terminal command UX via `/sessions`, with previews, interruption/checkpoint filters, smart `/resume` targeting including `pending`, and `/history` transcript peeks.
- Startup doctor now checks config presence, writable Colony paths, provider credentials, default cloud readiness, and port availability, with slash-command filters for errors, warnings, and search.
- Provider status now surfaces observed circuit/failover health from real runs without extra probe cost, plus focused slash-command drill-down by provider.
- Status/workspace command UX now includes observed provider health summary, latest failover line, and workspace start-dir visibility.
- Prompt assembly now carries richer workspace/runtime truth, including workspace start-dir, circuit state, provider health, and recent failovers.
- Ink operator surface now shows workspace identity, startup doctor counts, provider health, recent failovers, and recent saved sessions during live runs.
- Caveman outbound compression bridge present for non-local LLM hops while canonical transcript truth remains verbatim.
- Structured reusable fact memory now persists scoped decision/constraint/pattern/preference/ownership recall beside transcript truth and derived artifacts.
- Async-safe MemPalace store/layer/graph foundation now avoids sync filesystem helpers in active memory stack paths.
- Exact-vs-derived recall controls, query-intent-aware ranking, and MemPalace hall/wing/room/source-file routing are now substantially advanced.
- MemPalace exact recall now supports nearby, broader, and cross-wing related traversal with conservative hall fallback and source-file closet truth.
- `/memory plan <query>` now previews inferred memory truth mode, exact/derived recall controls, MemPalace distance threshold labels, graph-hop bounds, sanitized filter controls, precision diagnostics, section ordering, intent tags, palace route hints, and structured durable-memory focus/hints/boost labels without reading memory stores or echoing raw query text.
- Real-session memory/MemPalace recall QA now verifies exact transcript truth separation, derived ownership/decision visibility, palace issue/path routing, procedure routing, bounded query hashes, body-safe diagnostics, section ordering, and exact-only shown-count accounting.
- Ownership/responsibility memory extraction now supports `who owns` operator recall with durable fact ranking.
- `/channels external` exposes redacted external-adapter registration/config gate state and pure Slack/Discord/Telegram event-normalization helper truth; `/channels external register <channel> <approval-signature>` can emit a credential-free host-mediated registration action for an already accepted plan; `/channels external webhook telegram <approval-signature>` can emit a credential-free Telegram webhook setup action; host-owned execution can register adapters against supplied registry/private candidate state; host-owned dispatch can route normalized Slack/Discord/Telegram text events through an injected session bridge when host auth policy accepts the event; host-owned external vendor webhook transport can accept verified vendor-shaped HTTP events into that bridge; host-owned Telegram webhook setup can make one injected `setWebhook` call for an approved private candidate; signed Slack `url_verification` challenges can return only the challenge before host-auth/bridge dispatch; signed Discord Interactions `PING` requests can return only `{ "type": 1 }` after injected signature verification before host-auth/bridge dispatch; `/channels external subscribe slack <approval-signature>` can stage a credential-free Slack subscription setup handoff for an accepted plan; Slack setup plans expose redacted handoff checklist and retry-mode truth without claiming live execution; Slack and Discord subscription plans now expose redacted credential setup readiness with required credential-ref labels, host-supplied runtime secret responsibilities, missing/invalid ref status, and no-persistence/no-default-live-delivery guardrails; a host-owned executor can perform one injected Slack `apps.manifest.update` call for an approved private Slack candidate with host-supplied manifest/token; Slack setup now supports either `message.channels` with `channels:history` or mention-only `app_mention` with `app_mentions:read`, but not mixed Slack event subscriptions; approved Slack setup can append redacted durable event binding records, and signed/host-authenticated Slack callbacks can be fail-closed before bridge acceptance unless a supplied binding reader/list matches workspace and event type; signed and host-authenticated duplicate Slack `message`/`app_mention` retries are suppressed through bounded in-memory route/message-id tracking and receive bounded 202 duplicate ACKs without route message-count increment, second runner dispatch, second adapter reply, durable dedupe persistence, or retry worker; inbound-only Slack `file_share` and file-bearing `app_mention` callbacks now preserve bounded redacted file metadata without fetching, downloading, uploading, previewing, or persisting private file URLs; approval-bound external media transfer handoff can pass only safe internal file refs and bounded metadata to an injected host handler, now with a domain-separated stable transfer key, source-ref fingerprints for host idempotency/audit, transfer-key-bound host result success, manual operator retry metadata for retryable host-owned media transfer failures, bounded host-reported success receipt metadata that is not Colony-verified delivery or file inspection truth, an opt-in foreground host media transfer worker factory with injected host source resolution plus vendor send capabilities, opt-in source-resolution timeout handling with cooperative abort/manual retry metadata before vendor send, opt-in vendor-send timeout handling with cooperative abort/manual retry metadata after source resolution, opt-in bounded foreground source-resolution retry before vendor send, sanitized aggregate and per-file foreground source-retry metadata, request-only source attempt context for host resolvers, request-only previous source-failure context for retrying host resolvers, request-only vendor-send attempt/no-automatic-retry context plus request-only target context for host senders, sanitized manual retry safety context, durable manual retry revalidation handoff truth for fresh approval/source-ref checks and no stale resolved-file reuse, and stage-specific manual retry verification scope, stage-derived manual retry reason codes, stage-derived manual retry operator actions, durable source-ref fingerprint handoff, manual retry backoff hints and backoff correlation, approval replay truth, target-correlation fingerprints, bounded work-item descriptors, deterministic work-item correlation ids, and preflight-only work-item replay validation for source-stage versus vendor-stage reinvoke decisions, and fingerprint-only source revalidation planning for accepted retry replay host actions; `/channels external subscribe discord <approval-signature>` can stage a credential-free Discord Interactions endpoint setup handoff for an accepted plan; a host-owned executor can perform one injected Discord Edit Current Application call for an approved private Discord candidate to set only `interactions_endpoint_url`; a host-owned executor can perform one injected Discord guild application-command bulk overwrite for an approved private Discord candidate with one to five approval-bound chat-input commands; and retryable Slack subscription setup failures expose manual operator reinvoke metadata by default with opt-in bounded foreground retry metadata when requested. Durable redacted manual retry audit records, trusted audit-record-bound retry-ledger entry plans, append-only redacted retry-ledger entry persistence, trusted retry-ledger entry preflight, retry-ledger-preflight-bound manual retry control readiness plans, supplied retry-control readiness preflight, host/operator retry-control handoff descriptors, supplied operator handoff preflights, worker-selection intent descriptors, branded host-supplied worker handler-readiness descriptors, worker invocation handoffs, trusted foreground invocation execution, non-persistent invocation-execution receipt descriptors, supplied receipt preflights, and trusted closure retry-ledger entry worker invocation handoffs are enabled only for the verified manual retry audit chain; no retry worker/schedule, default live credentials, public hosting, broader Slack automatic retry workers, broader Discord command/live-delivery UX beyond the guild command executor, Colony-owned default uploads/downloads/previews, private file URL persistence, background retry workers, privileged Gateway intents, broader slash-command lifecycle/UX, adapter attachment delivery, Colony-verified late vendor delivery, vendor-send retry after an ambiguous send attempt, or default live inbound vendor delivery is enabled.
- Antelligence launch framework docs now define The Colony's enterprise operating-model narrative with a canonical playbook, launch checklist, and reusable enterprise templates. This is a framework and launch-readiness asset, not a claim that enterprise admin, cloud, mobile, IDE, or live external-channel automation surfaces are shipped.
- Launch Alpha 0 docs and gate are present: root README source+Bun onboarding, `docs/LAUNCH_ALPHA_0.md`, `verify:alpha0`, and claim-safety checks.
- `/swarm llm "<objective>"` can run real AgentLoop-backed planner/worker/reviewer stages with deterministic status, resume, retry, artifacts, token metadata, and durable snapshot truth; default `/swarm "<objective>"` remains coordinator-only for compatibility.
- `/swarm status <run_id>` now renders a stage timeline with started/ended times, attempt counts, token/cost metadata, artifact counts, redacted artifact review previews, latest failures, and retry history; phase 233 also locks injected failure coverage, restart-safe cancellation preservation, interrupted-stage resume history, and persisted approval-wait preservation across resume/retry.
- GitHub local workspace execution has an explicit-approval action plan plus an injected local git executor for bounded branch/worktree creation receipts and verified PR handoff artifacts; it still does not fetch GitHub, push, or create remote PRs by default.
- MCP/plugin package execution has an approval-gated install/update receipt path through an injected executor. It requires exact plugin trust approval, confines package paths, blocks lifecycle scripts unless `--ignore-scripts` is present, redacts executor output, stops after failed commands, and still does not fetch registries, activate live plugin records, start sidecars, or persist credentials.
- Plugin package planning can consume supplied registry metadata for checksum/integrity/signature summaries, rejects identity/source/digest mismatches before sidecar trust signatures are produced, redacts raw signatures and unsafe registry metadata, and package-driven sidecar activation can proceed only through an explicit approval plus matching completed install/update receipt plus injected supervisor. Registry metadata fetch now has an explicit high-risk approval-gated host/network handoff boundary and an exact-approval injected host-executor receipt with response/metadata validation. Package-code execution now requires a ready exact-approval preflight and calls only an injected host executor, producing redacted execution receipts. The built-in plugin marketplace view can inspect bundled descriptors read-only, marketplace registry fetch handoffs can describe the existing injected registry-fetch executor path for safe bundled entries, marketplace registry fetch execution status can project completed/failed/blocked/not-executed states from supplied receipts without running anything, approved marketplace registry fetch handoff execution can delegate only to the existing injected registry-fetch helper after matching handoff/action/approval validation, marketplace registry-fetch metadata planning can require a completed receipt plus host-supplied full metadata before passing registry metadata into the package planner, metadata-bound marketplace install/update handoffs can require a Phase 252 `metadata_ready` planning entry before producing a usable install/update handoff for that bundled entry, approved metadata-bound install/update handoff execution can require that ready metadata-bound gate before delegating to the existing injected install/update execution helper, metadata-bound install/update execution status can project metadata-blocked/not-executed/blocked/failed/completed states from Phase 253 handoffs and Phase 254 receipts without running anything, metadata-bound activation readiness can project completed metadata-bound install status into approval/handoff/active next-action states without starting anything, metadata-bound activation handoffs can describe the approval-bound injected supervisor handoff path for ready metadata-bound readiness entries without starting anything, approved metadata-bound activation handoff execution can delegate only after matching handoff/action/install-receipt/approval validation, metadata-bound activation execution status can project metadata-blocked/activation-not-ready/not-executed/blocked/failed/completed/active states from Phase 256 readiness and Phase 258 receipts without starting anything, a read-only lifecycle board can combine metadata planning, install/update status, activation readiness, and activation execution status into one operator view without running anything, lifecycle handoff can turn lifecycle-board states into bounded operator-only checklist actions with approval-required versus inspect-only summaries and optional state/cap filtering, lifecycle runbook can order lifecycle handoffs into bounded operator/reviewer steps with approval-focused filtering and cap controls, lifecycle approval packets can project approval-required runbook steps into bounded operator/reviewer approval packets with metadata/install/update/activation subject labels and prerequisite-aware ordering, lifecycle approval review can check those packets against supplied approval evidence into ready/missing/mismatch/expired states while denying direct host action, lifecycle approval handoff can project reviewed approval packets into operator-only handoff descriptors, lifecycle approval handoff preflight can project those handoffs into bounded host-handoff preflight descriptors while still denying direct host action, marketplace install/update handoffs can describe the existing injected install/update executor path for import/update actions, install/update execution status can project completed/failed/blocked/not-executed states from supplied receipts without running anything, approved install/update handoff execution can delegate only to the existing injected install/update helper after matching handoff/action/approval validation, activation readiness can explain review/install-receipt/approval/operator-handoff state without starting anything, approved activation handoff execution receipts can delegate to the existing injected supervisor activation helper only after matching handoff/action/receipt/approval validation, and activation execution status can project completed/failed/blocked/active/not-executed states from receipts without running anything. Colony still provides no built-in live registry fetch/install client, live/default install execution client UX, default live activation UX, transport creation in the activation helper, catalog mutation from the receipts, sidecar start from status views, or credential persistence.
- Lifecycle approval handoff preflight now projects reviewed approval handoffs into bounded host-handoff preflight descriptors with ready/blocker states, next-action guidance, host-handoff metadata redaction, state/cap filtering, and explicit host-action denial.
- Lifecycle host handoff request now projects approval handoff preflights into bounded operator/host request packets with explicit request commands, descriptor-only payloads, state/kind/cap filtering, blocker preservation, redaction, and explicit host-action denial.
- Lifecycle operator queue now groups host handoff request descriptors into approval-blocker, metadata, install/update, activation-readiness, and activation request lanes with operator actions, state/lane/cap filtering, redaction, and default-live-execution denial.
- Lifecycle default UX planning now projects operator queues into read-only prompt descriptors for metadata, install/update, activation-readiness, activation, approval collection, and approval refresh actions with stable command labels/ids, blocker preservation, redaction, and default-live-execution denial.
- Lifecycle default UX command palettes now project prompt plans into read-only operator command entries grouped as host handoff requests and approval remediation, with stable command previews, safety badges, blocker preservation, redaction, and default-live-execution denial.
- Lifecycle default UX command briefs now project command palettes into read-only operator brief lines grouped as ready commands and approval remediation, with request-bound copy text, display labels, blocker preservation, redaction, and default-live-execution denial.
- Lifecycle default UX command transcripts now project command briefs into read-only bounded terminal transcript lines and fixed safety summaries, with operator-only hints, blocker preservation, redaction, and default-live-execution denial.
- Lifecycle default UX command panels now project command transcripts into read-only operator panel cards grouped as ready commands, approval remediation, and safety summaries, with copy text, terminal hints, blocker preservation, redaction, and default-live-execution denial.
- Lifecycle default UX command panel digests now project command panels into read-only concise operator digest items grouped as ready commands, approval remediation, and safety summaries, with copy text, terminal hints, blocker preservation, redaction, and default-live-execution denial.
- Lifecycle default UX command panel digest clipboard packets now project command panel digests into read-only bounded clipboard packets grouped as ready commands, approval remediation, and safety summaries, with clipboard text, copy text, blocker preservation, redaction, and default-live-execution denial.
- Lifecycle default UX command panel digest clipboard review packets now project clipboard packets into read-only bounded operator/reviewer review checklists grouped as ready commands, approval remediation, and safety summaries, with review text, copy text, review-ready/blocker states, blocker preservation, redaction, and default-live-execution denial.
- Lifecycle default UX command panel digest clipboard review closeout packets now project review checklists into read-only bounded operator/reviewer closeout packets grouped as ready commands, approval remediation, and safety summaries, with closeout text, review-line preservation, copy text, closeout-ready/blocker states, blocker preservation, redaction, and default-live-execution denial.
- Lifecycle default UX command panel digest clipboard review closeout summary packets now project closeout packets into read-only bounded operator/reviewer summary packets grouped as ready commands, approval remediation, and safety summaries, with summary text, closeout/review-line preservation, copy text, summary-ready/blocker states, blocker preservation, redaction, and default-live-execution denial.
- Web control remains local-first and scoped; the operator shell renders provider/workflow/swarm/channel status, mutation handoff controls are opt-in, require `web.mutate`, reject non-local hosts, and do not execute actions directly.
- The current verification frontier is `verify:phase282` plus `tsc --noEmit`
  through `bun run verify:all`; `verify:alpha0` remains the focused Launch
  Alpha 0 gate.

## What This Means

The project is no longer blocked on "can it answer a question?" That milestone is closed. Big remaining gap is not basic runtime viability. Big remaining gap is full Colony breadth and product completeness.

## Execution Order

Work proceeds in this order:

1. Phase 0 - Reality Sync and Control Board
2. Phase 1 - Single-Agent Product Core
3. Phase 2 - Memory Foundation
4. Phase 3 - Workflow Engine
5. Phase 4 - Multi-Agent Colony Core
6. Phase 5 - Tools, Skills, and MCP
7. Phase 6 - Gateway and Multi-Surface Delivery
8. Phase 7 - Hardening, Packaging, and Release

## Phase 0 - Reality Sync and Control Board

Goal: Make docs, verification, and planning match actual codebase state.

Deliverables:

- Rewrite roadmap and gap analysis around actual TS state.
- Add deep execution plan with file targets, references, dependencies, and exit criteria.
- Add and maintain a benchmark board for capability-class parity vs Claude Code and OpenClaw.
- Add single-command project verification entry point.
- Define parity board by subsystem: complete, partial, deferred, or unbuilt.

Exit criteria:

- Planning docs no longer claim UI still uses simulated loop behavior.
- Help text, slash-command descriptions, and benchmark docs describe the same live runtime.
- Team has one ordered build sequence for all remaining major subsystems.
- `bun run verify:all` becomes standard pre/post-change gate.

Estimated effort: 2-3 days

## Phase 1 - Single-Agent Product Core

Goal: Finish everything required for "daily driver" single-agent use.

Focus:

- Gateway and slash-command completion.
- Better session status, workspace awareness, provider readiness, and startup diagnostics.
- Prompt/context plumbing across runtime, workspace, skills, and command surfaces.
- Approval UX hardening and interrupt recovery.
- Context visibility, compaction follow-through, and error recovery polish.
- Tool UX polish without broad tool-surface explosion.

Exit criteria:

- One agent can safely handle long interactive sessions with clear status, approvals, compaction, failover, and usable operator feedback.
- Core slash commands are complete and trustworthy.
- Common misconfiguration paths fail clearly instead of silently degrading.

Estimated effort: 1-2 weeks

## Phase 2 - Memory Foundation

Goal: Give Colony durable continuity across turns and sessions.

Current baseline already shipped:

- JSONL transcript logging with secret sanitization.
- Searchable session recall.
- Hybrid memory retrieval with exact transcript recall kept separate from derived compact artifacts.
- Compaction-to-memory handoff so compacted turns are preserved before removal from live context.
- Structured reusable fact extraction with scope/category metadata and cross-session dedup.
- Async-safe MemPalace store/layer/graph foundation for wake-up, recall, search, and navigation paths.

Focus:

- Local memory write/read pipeline.
- Session recall and searchable history.
- Stronger entity extraction and mission-critical fact retention.
- Ownership/responsibility recall is covered by `verify:phase63`.
- Resolution/what-fixed-it recall is covered by `verify:phase64`.
- Finish remaining operator surfaces and inspectability around memory routing/truth modes.
- Complete MemPalace hierarchy hardening and remaining parity gaps.
- Keep compaction-to-memory handoff stable while phase closes.
- `verify:phase62` locks body-safe memory query-plan inspection before retrieval, including structured ranking preview, sanitized unknown-mode errors, and bounded category matching.
- `verify:phase63` locks durable ownership/responsibility fact extraction/ranking, loaded/saved structured metadata sanitization, loaded markdown memory sanitization, and structured-boost admission gating.
- `verify:phase64` locks durable resolution diagnostic extraction/ranking and memory-context surfacing for `what fixed it` recall.
- `verify:phase16` locks compact artifact load sanitization before derived recall.

Exit criteria:

- Colony can recover useful prior context across sessions without manual copy/paste.
- Compaction no longer acts like context loss; it becomes context distillation.

Estimated effort: 2-3 weeks

## Phase 3 - Workflow Engine

Goal: Add resumable multi-step execution under the chat interface.

Focus:

- Workflow DAG definitions.
- Run records, checkpoints, retries, resumability, artifact tracking.
- Approval checkpoints inside workflow steps.
- Budget-aware and failure-aware execution.

Current shipped baseline:

- DAG validation, JSON run persistence, checkpoints, retry attempts, approval pause/resume, and artifact tracking exist in `src/workflow/*`.
- `/workflow` and `/status runtime` expose workflow summaries when runtime workflow snapshots exist.
- Step budget gating and required-approver policy enforcement exist through the workflow engine.
- Runtime-compatible workflow lifecycle hooks and run-to-runtime snapshot conversion exist through `WorkflowRuntimeRunner`.
- AgentLoop-backed workflow task handlers and reusable task-step templates exist through `createAgentLoopWorkflowHandler` and `createAgentLoopTaskStep`.
- Packaged workflow libraries for linear AgentLoop and approval-gated delivery workflows exist.
- Serializable workflow automation control commands exist through `WorkflowAutomationController`.
- Daemon control-plane host integration exists through `DaemonControlPlaneHost`; HTTP transport/client integration and typed remote session/workflow actions exist through `DaemonHttpControlPlaneServer` and `DaemonControlPlaneClient`; remote approval operator UX and broader task automation remain open.

Exit criteria:

- Colony can execute structured jobs, pause, resume, retry, and audit them.

Estimated effort: 2-3 weeks

## Phase 4 - Multi-Agent Colony Core

Goal: Turn single-agent runtime into real colony orchestration.

Focus:

- Agent spawning and worker lifecycle.
- Coordinator, work queue, and inter-agent messaging.
- Caste-aware role assignment.
- Shared budget, approval, and context policies.
- UI for swarm visibility.

Current shipped baseline:

- `ColonyAgentRegistry` spawns caste-aware planner/worker/reviewer-style workers with per-worker `AgentSession` instances.
- worker snapshots expose role, caste, objective, parent agent, tenant scope, lifecycle state, task context, result context, and session metadata without transcript history.
- worker lifecycle operations support ready/start/pause/resume/complete/stop with ordered lifecycle events.
- `verify:phase24` locks spawn/session creation, role/caste filtering, lifecycle transitions, event ordering, and invalid-transition protection.
- `ColonyCoordinator` provides prioritized task queueing, worker task claiming, completion/failure state, and direct worker mailbox messaging.
- `verify:phase25` locks queue assignment, worker lifecycle integration, task completion/failure, and direct inter-agent mailbox messaging.
- `ColonyCoordinator` also provides fan-out execution records, scoped child task dispatch, fan-in result aggregation, and cancellation of queued/assigned execution children.
- `verify:phase26` locks fan-out dispatch, fan-in aggregation, and execution cancellation behavior.
- `ColonyCoordinator` task snapshots carry budget, approval, security intent, awaiting-approval state, and policy decision telemetry; dispatch fails closed for denied budget/security and blocks approval-gated work until the required approver approves it.
- `verify:phase27` locks budget caps, approval-gated dispatch, required-approver enforcement, and shared security policy evaluation before worker assignment.
- `ColonySwarmRuntime` starts objective-scoped planner/worker/reviewer swarm runs over coordinator fan-out execution records, `/swarm` supports start/status/cancel operator actions, and optional `JsonSwarmRunStore` persistence records restart-safe snapshot truth for run state, workers, tasks, artifacts, approvals, and cancellation.
- `verify:phase28` locks `/swarm` runtime start/inspect, cancellation, command payloads, and command executor routing.
- `verify:phase87` locks durable swarm snapshots, restart-safe load/status truth, persisted cancellation of restored runs, worker/task/result/failure/artifact/approval metadata preservation, and malformed journal fail-closed loading.

Exit criteria:

- Planner/worker/reviewer patterns operate predictably inside one session.
- Multi-agent execution is inspectable, cancellable, and policy-bound.

Estimated effort: 3-5 weeks

## Phase 5 - Tools, Skills, and MCP

Goal: Expand capability surface only after core runtime is stable.

Focus:

- Missing high-value tools.
- Skill loading and selection.
- Workspace-driven tool enablement.
- MCP server/client foundations.
- Uniform schema, approval, and externalization behavior.

Current baseline already shipped:

- `SkillCatalog` discovers local `SKILL.md` files asynchronously without executing adjacent scripts or assets.
- skill metadata includes description, caste, tags, required tools, approval requirements, trust level, and body instructions.
- bounded skill prompt instruction blocks can feed the existing prompt assembly path.
- `/skills`, `/skills search`, and `/skills inspect` expose the loaded skill catalog to operators.
- `InProcessMcpServer` and `InProcessMcpClient` provide a minimal JSON-RPC MCP foundation for initialize, `tools/list`, and `tools/call`.
- `McpTransport` and `InProcessMcpTransport` provide a transport-agnostic JSON-RPC bridge so the client no longer depends directly on an in-process server.
- `GuardedMcpTransport` provides the mandatory local policy wrapper used by shipped stdio, HTTP, and plugin sidecar MCP boundaries: bounds, timeout, concurrency, method/tool allowlists, origin/plugin allowlists, and bearer-token checks.
- `McpToolAdapter` exposes current `ToolRegistry` tools through MCP metadata and routes calls through `ToolExecutor` with fail-closed, verifier-owned exact approval proof behavior.
- `verify:phase30` locks skill discovery, selection, prompt bounding, parser truth, and operator rendering.
- `verify:phase31` locks MCP initialize/list/call, protocol error handling, registry adapter behavior, approval-gated tool denial, and verifier-approved exact proof execution.
- `verify:phase65` locks MCP approval-proof hardening so caller-supplied approval booleans and fake/mismatched proofs cannot execute approval-gated tools.
- `verify:phase66` locks generic MCP transport client behavior, fail-closed response validation, sanitized transport errors, and approval proof across the transport seam.
- `verify:phase67` locks guarded MCP transport boundary behavior before real stdio, HTTP, or plugin transports are exposed.
- `verify:phase68` locks server-side MCP JSON-RPC error redaction before external clients can receive raw server responses.
- `verify:phase69` locks method-specific MCP result-shape validation for initialize, tools/list, and tools/call responses plus result-array hardening before real external transports are exposed.
- `verify:phase70` locks the first guarded stdio MCP transport foundation: Bun pipe spawning without shell invocation, guarded client flow, bounded/redacted stderr diagnostics, timeout cleanup, and fail-closed response id matching.
- `verify:phase71` locks trusted stdio MCP server configuration: exact operator trust signatures, absolute-command policy, shell/package-runner/inline-eval rejection, secret env rejection, guard context injection, and list-only behavior without local tool allowlists.
- `verify:phase72` locks richer trusted stdio MCP operator/audit surfaces: redacted config inspection and startup trust event projection without spawning local code or leaking argv/env values.
- `verify:phase73` locks the internal fetch-backed HTTP MCP transport foundation: HTTPS-only endpoints, SSRF host rejection, redirect-disabled POST JSON-RPC flow, safe request cloning, bounded response reads, sanitized failures, abortable timeouts, and fail-closed response-id validation.
- `verify:phase74` locks trusted HTTP MCP server configuration and operator/audit exposure: exact approval signatures, redacted approval requests, guarded client creation, allowlisted tool calls, stale approval rejection, and redacted HTTP trust events without raw transport exposure.
- `verify:phase75` locks trusted plugin MCP sidecar configuration: plugin-specific approval signatures, package/sidecar/capability trust inputs, guarded sidecar client creation, allowlisted tool calls, stale approval rejection, redacted plugin trust events, and no raw sidecar transport exposure.
- `verify:phase76` locks managed plugin MCP sidecar lifecycle: trusted connect handshake, signed protocol/server expectations, initialize-before-use, post-connect initialize replay rejection, lifecycle state, idempotent close, post-close send rejection, retryable/redacted close failures, failed-handshake cleanup, and redacted generic handshake failures.
- `verify:phase77` locks the plugin sidecar supervisor foundation: injected trusted transport factory, exact active-signature handling, direct-start backoff/limit enforcement, bounded restart/backoff policy, redacted in-memory lifecycle event projection, redacted lifecycle snapshots, list/inspect/client/stop behavior, retryable/redacted stop failures, and no plugin package install path.
- `verify:phase78` locks the dry-run plugin package discovery/import planner: manifest validation, digest/source/sidecar checks, deterministic import/update/keep/review/reject plans, normalized trusted sidecar candidates, exact approval request compatibility, redacted rejected metadata, and no package install, package execution, sidecar start, registry fetch, or package/catalog write path.
- `verify:phase79` locks durable redacted plugin package plan events: event projection from dry-run plans, append-only JSONL persistence, malformed journal fail-closed behavior, exact signature separation, high-entropy actor redaction, and no package install, package execution, sidecar start, registry fetch, or package/catalog write path.
- `verify:phase80` locks approval-gated plugin package catalog staging: explicit approval before staging writes, live catalog isolation, body-safe/redacted staged metadata, plugin-signature namespace enforcement, unknown sidecar-kind rejection, fail-closed malformed candidate loading, and no package install/update execution, package execution, sidecar start, registry fetch, or live package/catalog promotion path.
- `verify:phase81` locks `/memory plan` recall-control previews for exact/derived enablement, MemPalace distance threshold labels, graph-hop bounds, sanitized hall/wing/room/source filter controls, derived-only palace-search boundaries, and body-safe precision diagnostics.
- `verify:phase82` locks typed tool metadata contracts for normalized defaults, builtin read-only/destructive/concurrency/progress/transcript/search/persisted-result truth, metadata-derived runtime categories, MCP annotations, and `/tools` operator rendering.
- `verify:phase83` locks safe local tool breadth for `glob_find`, structured `git_status`, bounded/redacted `git_diff`, and approval-gated package `test_runner`/`lint_runner` wrappers.
- `verify:phase84` locks policy-gated `web_fetch` and `web_search` with approval-required open-world metadata, HTTPS-only network policy, DNS/private-host SSRF rejection, redirect-disabled fetches, text-only response handling, size bounds, timeout/abort wiring, redaction, untrusted-content wrapping, and prompt-injection signal reporting.
- `verify:phase85` locks MCP resource list/read support with protocol/client/server/resource-adapter surfaces, guarded resource URI allowlists, trusted stdio/HTTP/plugin resource policy signing/audit visibility, resource-aware package planning, strict result-shape validation, and operator metadata without routing resources through tool execution.
- `verify:phase86` locks plugin package live catalog promotion with second-approval promotion from staged candidates into live-disabled metadata, metadata-only rollback records, namespace/catalog isolation, malformed candidate rejection, durable live catalog fail-closed loading/appending, redaction, and no package install/update, package execution, sidecar start, registry fetch, or activation path.
- `verify:phase234` locks approval-gated plugin package install/update execution receipts with exact plugin trust approval, import/update-only execution, package-path confinement, `--ignore-scripts` lifecycle blocking, injected executor calls, redacted stdout/stderr previews, stop-after-failure ordering, and no registry fetch, live activation, sidecar startup, or credential persistence.
- `verify:phase235` locks supplied registry metadata enrichment for plugin package plans with checksum/integrity/signature summaries, raw signature redaction, identity/source/digest mismatch rejection before sidecar trust signatures, unsafe registry metadata rejection, compatibility without metadata, and no live registry fetch, activation, sidecar startup, package execution, or credential persistence.
- `verify:phase236` locks approval-gated package-driven sidecar activation controls through an injected supervisor after exact approval and a matching completed install/update receipt, including failed-supervisor receipt truth, signature tamper rejection, unknown-sidecar rejection, redaction, and no live registry fetch, default activation, package lifecycle execution, transport creation, or credential persistence.
- `verify:phase237` locks approval-gated plugin registry host/network handoff controls with exact signatures, HTTPS/no-credential/no-local URL validation, unsafe URL redaction, signature tamper blocking, and no Colony-owned network execution, package execution, activation, catalog mutation, or credential persistence.
- `verify:phase238` locks approval-gated injected host-executor registry metadata fetch receipts with HTTP/content-type/size/JSON/package identity validation, redaction, and no built-in Colony registry client, default marketplace fetch, package execution, activation, catalog mutation, or credential persistence.
- Live/default registry fetch/install UX, default/live plugin activation execution UX beyond readiness plus explicit approved supervisor handoff, and production plugin fabric beyond approved receipts remain open.

Exit criteria:

- Colony can operate on real repositories and external contexts through a safe, extensible interface.

Estimated effort: 2-3 weeks

## Phase 6 - Gateway and Multi-Surface Delivery

Goal: Make terminal UI one client, not whole system.

Focus:

- Gateway middleware pipeline.
- Daemon/control-plane layer.
- Session API and remote approvals.
- Channel adapter contracts, first outbound adapters, then explicit registration/config, inbound vendor semantics, media, retries, and voice.
- OpenClaw-inspired delivery patterns adapted to Colony security law.

Current baseline already shipped:

- `DaemonControlPlaneHost` exposes serializable session lifecycle and workflow delegation command envelopes.
- daemon session snapshots expose lifecycle/usage metadata without embedding transcript history.
- `verify:phase21` locks the transport-agnostic control-plane API foundation.
- `DaemonHttpControlPlaneServer`, `handleDaemonHttpRequest`, and `DaemonControlPlaneClient` provide the first HTTP listener/client path with optional bearer-token enforcement.
- `verify:phase22` locks HTTP validation, bearer-token enforcement, local listener startup, and remote client session calls.
- `DaemonControlPlaneClient` exposes typed remote session create/list/inspect/close and workflow list/start/inspect/approve actions.
- `verify:phase23` locks typed remote session recovery-style actions and remote workflow approval actions.
- `DaemonAuthPolicy` provides scoped bearer authorization for daemon/session/workflow commands, with redacted auth metadata exposed through `/daemon` status/auth/sessions views.
- `verify:phase29` locks scoped daemon auth, token redaction, parser truth, and `/daemon` operator visibility.
- `verify:phase89` locks the read-only web control shell: fail-closed dedicated `web.read` auth, GET-only HTML and JSON state routes, POST/mutation rejection, daemon/workflow/swarm/channel status-only projection, no arbitrary metadata exposure, no mutation endpoints, no form controls, cache-safe JSON headers, query credential redaction, and transcript/message/content/tool-output body redaction.
- `verify:phase91` locks first outbound Slack/Discord/Telegram adapter classes: injected raw `fetch`, Slack `chat.postMessage`, Discord channel/thread message posts, Telegram `sendMessage`, credential/query redaction across status/delivery/errors, and no default registration from contract fixtures.
- `verify:phase118` locks inbound-only Slack media metadata inspection: signed and host-authenticated Slack `file_share` callbacks enter the existing deferred ACK/session bridge path with bounded redacted file metadata and safe fallback text, and Slack `app_mention` callbacks with files preserve the same metadata without fetching, downloading, uploading, previewing, persisting private URLs, starting listeners, public hosting, retry workers, or default live inbound delivery.
- `verify:phase150` locks manual retry replay source revalidation checklists for external media transfer: accepted source-stage and vendor-stage replay source revalidation plans can produce deterministic host-owned operator checklists that order fresh approval verification, vendor-state verification when needed, per-source fresh resolution/no-stale-file checks, and final manual reinvoke. The checklist does not execute host handlers, resolve sources, fetch/download/upload/preview files, persist revalidation/checklist/work items, create retry ledgers, durable retry audit records, retry workers, schedules, public hosting, credential persistence, automatic vendor retry, or default live delivery, and it carries no raw refs, raw target identifiers, approval signatures, credentials, URLs, or file bytes.
- `verify:phase151` locks manual retry source revalidation checklist preflight for external media transfer: a supplied host/operator checklist is sanitized and compared against freshly recomputed approval-bound replay truth before manual reinvoke, stale approvals and malformed/tampered/current-truth-mismatched checklists fail closed, and no host handler, source resolution, file fetch/download/upload/preview, checklist persistence, retry ledger, durable audit record, retry worker, schedule, public hosting, credential persistence, automatic vendor retry, or default live delivery is created.
- `verify:phase152` locks manual retry source revalidation checklist acknowledgement templates for external media transfer: accepted checklist preflights can produce deterministic host-owned acknowledgement templates with one required acknowledgement per checklist step, vendor-state acknowledgement when required, stable approval-bound template ids, stale approval/malformed checklist rejection, and no host handler, source resolution, file fetch/download/upload/preview, acknowledgement/checklist persistence, retry ledger, durable audit record, retry worker, schedule, public hosting, credential persistence, automatic vendor retry, or default live delivery.
- `verify:phase153` locks manual retry source revalidation checklist acknowledgement preflights for external media transfer: supplied host/operator acknowledgements are sanitized and compared against freshly recomputed acknowledgement-template truth, accepting source/vendor acknowledgement readiness only when ids, checklist/template/work-item/transfer truth, source fingerprints, and step acknowledgements match, while rejecting stale approvals, execution/persistence claims, tampering, and raw-ref/target/signature/credential/URL/ledger leakage without executing host handlers, resolving files, creating retry ledgers, workers, schedules, public hosting, credential persistence, automatic vendor retry, or default live delivery.
- `verify:phase154` locks acknowledgement-bound manual retry host-action handoff after accepted source revalidation acknowledgement preflight: accepted source/vendor acknowledgements produce deterministic credential-free host-action readiness descriptors, stale approvals and tampered acknowledgements fail closed, action ids are stable and approval-bound, and no raw refs, target ids, signatures, credentials, URLs, retry ledgers, workers, schedules, public hosting, source resolution, host execution, automatic vendor retry, or default live delivery are created.
- `verify:phase155` locks source-revalidation result preflight after acknowledgement-bound manual retry host-action readiness: supplied host/operator source revalidation results are sanitized and compared against current acknowledgement-bound truth, accepting only fingerprint-only fresh-source confirmations for source/vendor paths while rejecting stale approvals, tampering, stale-file reuse claims, raw-ref/target/signature/credential/URL/ledger leakage, and any source resolution, host execution, persistence, retry worker, public hosting, automatic vendor retry, or default live delivery claim.
- `verify:phase156` locks source-revalidated host-action readiness after accepted source-revalidation result preflight: the deterministic manual reinvoke descriptor binds result/action/acknowledgement/checklist/work-item/source/target truth while rejecting stale approvals, tampering, raw-ref/target/signature/credential/URL/path/ledger leakage, and any source resolution, host execution, persistence, retry worker, public hosting, automatic vendor retry, or default live delivery claim.
- `verify:phase157` locks credential-readiness handoff truth after accepted source-revalidated host-action readiness: the descriptor binds the completed action/result/work-item/source/target truth, names only host-owned source-access and channel-send credential labels/responsibilities, and rejects stale approvals, tampering, raw-ref/target/signature/credential-value/URL/path/ledger leakage, source resolution, host execution, credential persistence, retry worker, public hosting, automatic vendor retry, or default live delivery claims.
- `verify:phase158` locks labels-only credential acknowledgement preflight after accepted credential readiness: supplied host acknowledgements are sanitized and compared against freshly recomputed readiness truth, accepting source/vendor credential labels only when they match and rejecting stale approvals, tampering, missing labels, credential values, raw refs, raw targets, signatures, URLs, paths, ledgers, source resolution, host execution, credential persistence, retry workers, public hosting, automatic vendor retry, or default live delivery claims.
- `verify:phase159` locks credential-acknowledged manual reinvoke host-action descriptors after accepted labels-only credential acknowledgement preflight: accepted source/vendor handoffs bind acknowledgement/readiness/action/result truth with sanitized deterministic ids and still reject stale approvals, invalid/value-bearing acknowledgement claims, raw refs, raw targets, signatures, URLs, paths, ledgers, source resolution, host execution, credential persistence, retry workers, public hosting, automatic vendor retry, or default live delivery claims.
- `verify:phase160` locks credential-acknowledged host-action preflights before manual reinvoke trust: supplied descriptors are sanitized and compared with freshly recomputed acknowledgement-bound truth, preserving source/vendor gates while rejecting stale approvals, tampering, execution claims, credential values, persistence, retry workers, public hosting, automatic vendor retry, or default live delivery claims.
- `verify:phase161` locks credential-acknowledged manual reinvoke request descriptors after accepted host-action preflight: accepted requests bind recomputed action/source/target/credential-label truth, preserve vendor-state gates, reject stale/tampered preflight inputs, and still create no execution, source resolution, persistence, credentials, retry workers, public hosting, automatic vendor retry, or default live delivery.
- `verify:phase162` locks credential-acknowledged manual reinvoke request preflights before later host trust: supplied request descriptors are sanitized and compared with freshly recomputed request truth, preserving source/vendor gates while rejecting stale approvals, tampering, execution claims, credential values, persistence, retry workers, public hosting, automatic vendor retry, or default live delivery claims.
- `verify:phase164` locks the preflight-only guard for supplied credential-acknowledged manual reinvoke handoff descriptors after accepted request preflight: supplied handoffs are sanitized and compared against recomputed request/source/target/credential-label truth while still rejecting execution, source resolution, credential values, persistence, retry workers, public hosting, automatic vendor retry, and default live delivery claims.
- `verify:phase165` locks opt-in host-owned credential-acknowledged manual reinvoke execution after accepted handoff preflight: source-stage reinvoke resolves sources freshly through injected host capabilities, vendor-stage reinvoke requires an explicit handoff-bound host vendor-state verification descriptor, missing or unregistered worker handlers and tampered/value-bearing/execution-claim handoffs fail closed before handler execution, and the path persists no credentials, creates no background retry worker/schedule, enables no public hosting, performs no default live delivery, and performs no automatic vendor retry.
- `verify:phase166` locks bounded credential-acknowledged manual reinvoke execution receipt preflight after accepted opt-in execution: source and vendor-stage receipts bind current handoff/request/transfer/source/target truth plus sanitized host delivered-count and transfer-key echo truth, vendor receipts require verified vendor-state execution truth, failed or tampered execution records fail closed, and the path persists no credentials, creates no retry ledger or durable retry audit record, creates no background retry worker/schedule, enables no public hosting, performs no default live delivery, and performs no automatic vendor retry.
- `verify:phase167` locks supplied credential-acknowledged manual reinvoke execution receipt preflight before later host trust: supplied receipts are sanitized and compared with freshly recomputed receipt truth, source and vendor-stage receipt preflights bind ids, source/target fingerprints, delivered-count, transfer-key echo, and vendor-state truth, tampered or contaminated receipt descriptors fail closed, and the path persists no credentials or receipts, creates no retry ledger or durable retry audit record, creates no background retry worker/schedule, enables no public hosting, performs no default live delivery, and performs no automatic vendor retry.
- `verify:phase168` locks receipt-preflight-bound manual retry closeout readiness after accepted receipt preflight: closeout descriptors bind current receipt/work-item/transfer/delivered-count/vendor-state truth while explicitly not claiming durable Colony closure, persistence, retry ledgers, durable audit records, public hosting, default live delivery, credential storage, or automatic vendor retry.
- `verify:phase169` locks supplied receipt-closeout preflight before later durable closeout trust: supplied closeout descriptors are sanitized and compared against freshly recomputed closeout truth while rejecting tampering, durable Colony closure claims, retry ledgers, durable audit records, retry workers, retry schedules, public hosting, default live delivery, credential storage, and automatic vendor retry.
- `verify:phase170` locks closeout-preflight-bound record-plan descriptors before later durable closeout persistence: accepted supplied-closeout preflights can produce a deterministic record plan that binds closeout/receipt/work-item/transfer/delivered-count/vendor-state truth while still closing no work item, persisting no closeout record, creating no retry ledger, durable audit record, retry worker, retry schedule, public hosting, default live delivery, credential storage, or automatic vendor retry.
- `verify:phase171` locks supplied closeout record-plan preflight before later durable closeout persistence trust: supplied record-plan descriptors are sanitized and compared against freshly recomputed record-plan truth while rejecting tampering, work-item closure, closeout persistence, retry ledgers, durable audit records, retry workers, retry schedules, public hosting, default live delivery, credential storage, and automatic vendor retry.
- `verify:phase172` locks record-plan-preflight-bound closeout record draft truth before later durable closeout persistence: accepted supplied record-plan preflights can produce deterministic draft descriptors that bind record-plan/closeout/receipt/work-item/transfer/delivered-count/vendor-state truth while still rejecting tampering and creating no persistence, work-item closure, retry ledger, durable audit record, retry worker, retry schedule, public hosting, default live delivery, credential storage, or automatic vendor retry.
- `verify:phase173` locks durable redacted manual retry closeout record persistence: accepted record-plan-preflight-bound drafts can be appended to a JSONL store and reloaded without raw ref/target/signature/credential/URL/path/ledger leakage, while still creating no work-item closure, receipt or credential persistence, retry ledger, durable retry audit, retry worker/schedule, default live delivery, public hosting, or automatic vendor retry.
- `verify:phase174` locks persisted-closeout-record-bound manual retry work-item closure: trusted persisted closeout records can produce deterministic closure descriptors that bind closeout-record persistence/work-item/transfer/delivered-count/vendor-state truth while rejecting copied or forged records and still creating no receipt or credential persistence, retry ledger, durable retry audit, retry worker/schedule, default live delivery, public hosting, or automatic vendor retry.
- `verify:phase175` locks append-only manual retry work-item closure persistence: trusted closure descriptors can be persisted as redacted records that bind the closure id, closeout-record persistence, work-item, transfer, delivered-count, source/target fingerprint, and vendor-state truth while rejecting tampered journals and append-failed captured records. It still creates no receipt or credential persistence, retry ledger, durable retry audit, retry worker/schedule, default live delivery, public hosting, or automatic vendor retry.
- `verify:phase176` locks supplied work-item closure persistence preflight: trusted persisted closure records are recomputed from trusted closeout record truth before downstream trust, copied/forged records and mismatched trusted records fail closed, and no receipt/credential persistence, retry ledger, durable retry audit, retry worker/schedule, default live delivery, public hosting, or automatic vendor retry is created.
- `verify:phase177` locks closure-preflight-bound manual retry audit record planning: accepted trusted closure-persistence preflights can produce deterministic redacted audit record plans with closure, closeout, receipt, work-item, transfer, delivered-count, source/target fingerprint, and vendor-state truth while still creating no durable retry audit record, retry ledger, retry worker/schedule, default live delivery, public hosting, receipt/credential persistence, or automatic vendor retry.
- `verify:phase178` locks supplied audit-record-plan preflight: host/operator supplied manual retry audit plans are sanitized and compared against recomputed trusted closure-persistence truth before later durable audit trust, with tampered, copied-closure, contaminated, or mismatched plans rejected and no durable retry audit record, retry ledger, retry worker/schedule, default live delivery, public hosting, receipt/credential persistence, or automatic vendor retry created.
- `verify:phase179` locks append-only redacted durable audit-record persistence: accepted audit-record-plan preflights can persist and reload deterministic manual retry audit records with load-time normalization/integrity checks, while still creating no retry ledger, retry worker/schedule, receipt or credential persistence, public hosting, default live delivery, or automatic vendor retry.
- `verify:phase180` locks trusted audit-record-bound retry-ledger entry planning: trusted persisted manual retry audit records can produce deterministic retry-ledger entry plans that bind audit record, audit plan, closure persistence, closeout record, receipt, handoff, request, replay action, transfer, delivered count, source/target fingerprints, and provenance truth, while still creating no retry ledger, retry worker/schedule, receipt or credential persistence, public hosting, default live delivery, or automatic vendor retry.
- `verify:phase181` locks append-only redacted retry-ledger entry persistence: trusted retry-ledger entry plans can persist and reload deterministic retry-ledger entries with load-time normalization/integrity checks, while still creating no retry worker/schedule, receipt or credential persistence, public hosting, default live delivery, or automatic vendor retry.
- `verify:phase182` locks trusted retry-ledger entry preflight: supplied persisted retry-ledger entries are normalized, integrity-checked, and accepted only when they are exact trusted runtime entries before downstream retry-control trust, while copied/tampered/append-failed captured entries fail closed and no retry worker/schedule, receipt or credential persistence, public hosting, default live delivery, or automatic vendor retry is created.
- `verify:phase183` locks manual retry control readiness planning: accepted trusted retry-ledger entry preflights produce deterministic retry-control readiness descriptors bound to ledger entry, ledger plan, audit record, closure persistence, closeout persistence, transfer, source/target fingerprint, delivered-count, vendor-state, and persistence provenance truth without creating retry workers, retry schedules, background retries, credential persistence, public hosting, default live delivery, or automatic vendor retry.
- `verify:phase184` locks manual retry control readiness plan preflight: supplied readiness descriptors are normalized and recomputed from trusted retry-ledger entry preflight truth before downstream retry-control trust, with mismatched, contaminated, copied-ledger, and append-failed-ledger inputs rejected without creating retry workers, retry schedules, background retries, credential persistence, public hosting, default live delivery, or automatic vendor retry.
- `verify:phase185` locks manual retry control operator handoff: accepted readiness preflights can produce deterministic host/operator retry-control handoff descriptors while mismatched, contaminated, copied-ledger, and append-failed-ledger inputs reject without selecting or creating retry workers, retry schedules, background retries, credential persistence, public hosting, default live delivery, or automatic vendor retry.
- `verify:phase186` locks manual retry control operator handoff preflight: supplied handoff descriptors are normalized and compared against recomputed readiness-preflight-bound handoff truth while mismatched, contaminated, copied-ledger, and append-failed-ledger inputs reject without selecting or creating retry workers, retry schedules, background retries, credential persistence, public hosting, default live delivery, or automatic vendor retry.
- `verify:phase187` locks manual retry control worker-selection intent: descriptors are created and preflighted from recomputed operator-handoff-preflight truth, bind source/target/vendor/persistence provenance plus `host_owned_foreground_manual_reinvoke`, and still reject mismatched or contaminated worker/schedule/credential/live-delivery claims without creating retry workers, schedules, background retries, credential persistence, public hosting, default live delivery, or automatic vendor retry.
- `verify:phase188` locks manual retry control worker handler readiness: descriptors are created and preflighted only after recomputed worker-selection preflight truth and a branded host-supplied foreground manual reinvoke worker handler, while missing/unbranded handlers, provenance tampering, and contaminated worker/schedule/credential/live-delivery claims fail closed without executing the handler or creating retry workers, schedules, background retries, credential persistence, public hosting, default live delivery, or automatic vendor retry.
- `verify:phase189` locks manual retry control worker invocation handoff: descriptors are created and preflighted only after recomputed handler-readiness preflight truth, record host execution as required but not attempted, and reject tampered worker/schedule/credential/live-delivery claims without executing the handler or creating retry workers, schedules, background retries, credential persistence, public hosting, default live delivery, or automatic vendor retry.
- `verify:phase190` locks manual retry control worker invocation execution: an accepted invocation handoff can drive explicit trusted host-owned foreground worker execution after recomputed preflight, fresh approval, transfer-key match, branded handler checks, and vendor-state verification for vendor-stage retries, while rejecting missing/untrusted handlers, stale approvals, stale candidates, and tampered handoffs before execution and still creating no retry worker, schedule, background retry, credential persistence, public hosting, default live delivery, or automatic vendor retry.
- `verify:phase191` locks manual retry control worker invocation execution receipt truth: accepted host-owned invocation executions can produce deterministic redacted non-persistent receipt descriptors after recomputed invocation-handoff preflight, binding transfer-key echo, host-reported delivery count/metadata presence, and vendor-state verification while rejecting tampered or failed executions and still creating no retry worker, schedule, receipt persistence, credential persistence, public hosting, default live delivery, background retry, or automatic vendor retry.
- `verify:phase192` locks manual retry control worker invocation execution receipt preflight: supplied receipt descriptors are sanitized and compared against recomputed current receipt truth, while mismatched receipts, contaminated receipts, and post-trust execution mutations reject without receipt persistence, retry worker scheduling, background retry, credential persistence, public hosting, default live delivery, or automatic vendor retry.
- `verify:phase193` locks manual retry control worker invocation execution receipt closeout readiness: accepted receipt preflights can produce deterministic non-persistent closeout descriptors that bind receipt, invocation handoff, handler readiness, worker selection, operator handoff, readiness-plan, retry-ledger, source/target fingerprints, delivered-count, receipt metadata, and vendor-state truth, while mismatched receipts, contaminated receipts, and post-trust execution mutations reject without new retry-ledger/audit persistence, receipt persistence, retry worker scheduling, background retry, credential persistence, public hosting, default live delivery, or automatic vendor retry.
- `verify:phase194` locks manual retry control worker invocation execution receipt closeout preflight: supplied closeout descriptors are recomputed against current receipt-preflight-bound closeout truth, while mismatched closeouts, contaminated closeouts, and post-trust execution mutations reject without new retry-ledger/audit persistence, receipt persistence, retry worker scheduling, background retry, credential persistence, public hosting, default live delivery, or automatic vendor retry.
- `verify:phase195` locks manual retry control worker invocation execution receipt closeout record planning: accepted closeout preflights can produce deterministic non-persistent closeout record plans, while mismatched closeouts, contaminated closeouts, and post-trust execution mutations reject without closeout-record persistence, new retry-ledger/audit persistence, receipt persistence, retry worker scheduling, background retry, credential persistence, public hosting, default live delivery, or automatic vendor retry.
- `verify:phase196` locks supplied manual retry control worker invocation execution receipt closeout record-plan preflight: supplied record plans are sanitized and compared against freshly recomputed record-plan truth, while mismatched plans, contaminated plans, and post-trust execution mutations reject without closeout-record persistence, new retry-ledger/audit persistence, receipt persistence, retry worker scheduling, background retry, credential persistence, public hosting, default live delivery, or automatic vendor retry.
- `verify:phase197` locks manual retry control worker invocation execution receipt closeout record draft truth: accepted supplied record-plan preflights can produce deterministic non-persistent closeout record drafts that bind record-plan, closeout, receipt, worker-selection, readiness, retry-ledger, source/target fingerprint, delivered-count, receipt-metadata, and vendor-state truth while rejecting mismatched plans, contaminated plans, and post-trust execution mutations without closeout-record persistence, new retry-ledger/audit persistence, receipt persistence, retry worker scheduling, background retry, credential persistence, public hosting, default live delivery, or automatic vendor retry.
- `verify:phase198` locks manual retry control worker invocation execution receipt closeout record persistence truth: accepted record-plan-preflight-bound drafts can be appended as redacted durable closeout records through JSONL, with tampered journals failing closed and no work-item closure, receipt persistence, new retry ledger/audit, retry worker scheduling, background retry, credential persistence, public hosting, default live delivery, or automatic vendor retry.
- `verify:phase199` locks supplied retry-control closeout record persistence preflight truth: trusted persisted closeout records are recomputed before downstream use, while copied, tampered, contaminated, and append-failed records are rejected without creating work-item closure, receipt persistence, retry ledgers, audit records, retry workers, schedules, public hosting, default live delivery, or automatic vendor retry.
- `verify:phase200` locks retry-control closeout-record persistence preflight-bound manual retry work-item closure: accepted trusted preflights produce deterministic non-persistent closure descriptors that bind closeout-record persistence, closeout record, record-plan, closeout, receipt, invocation handoff, handler readiness, worker selection, operator handoff, readiness plan, retry-ledger entry, source/target fingerprints, delivered-count, receipt metadata, vendor-state truth, and close provenance while copied, tampered, contaminated, and append-failed records fail closed without closure persistence, receipt persistence, new retry ledgers/audits, retry workers, schedules, public hosting, default live delivery, or automatic vendor retry.
- `verify:phase201` locks append-only retry-control work-item closure persistence: accepted preflight-bound closure descriptors can be persisted as redacted closure records with load-time integrity while still creating no receipt persistence, new retry ledger/audit, retry worker scheduling, background retry, credential persistence, public hosting, default live delivery, or automatic vendor retry.
- `verify:phase202` locks supplied retry-control work-item closure persistence preflight: trusted persisted closure records are recomputed before downstream use, while copied, tampered, contaminated, and append-failed records are rejected without creating receipt persistence, new retry ledger/audit, retry workers, schedules, public hosting, default live delivery, or automatic vendor retry.
- `verify:phase203` locks closure-persistence-preflight-bound retry-control work-item closure audit-record planning: accepted trusted closure-persistence preflights can produce deterministic non-persistent audit-record plans bound to closure, closeout, receipt, invocation, retry-control, fingerprint, provenance, and safety truth while still creating no durable audit record, new retry ledger, retry worker, schedule, credential persistence, public hosting, default live delivery, background retry, or automatic vendor retry.
- `verify:phase204` locks supplied retry-control work-item closure audit-record-plan preflight: supplied plans are normalized and compared against freshly recomputed closure-persistence-preflight-bound plan truth while mismatched, copied/untrusted, contaminated, and malformed plans fail closed without returning a trusted plan or creating durable audit records, new retry ledgers, retry workers, schedules, credential persistence, public hosting, default live delivery, background retry, or automatic vendor retry.
- `verify:phase205` locks retry-control work-item closure audit-record persistence: accepted audit-record-plan preflights can append redacted durable audit records to a dedicated JSONL store with deterministic ids, load integrity, tampered-journal rejection, missing-store and append-failure rejection, copied closure-persistence rejection, and no new retry ledger, retry worker, schedule, credential persistence, public hosting, default live delivery, background retry, automatic vendor retry, receipt persistence, or raw host-data persistence.
- `verify:phase206` locks supplied retry-control work-item closure audit-record persistence preflight: trusted supplied audit records are recomputed before downstream use, while copied, tampered, contaminated, and append-failed records are rejected without creating retry ledgers, retry workers, schedules, credential persistence, public hosting, default live delivery, background retry, automatic vendor retry, receipt persistence, or raw host-data persistence.
- `verify:phase207` locks trusted preflight-bound retry-control work-item closure retry-ledger entry planning: only trusted recomputed audit-record persistence preflight output can create a deterministic retry-ledger entry plan, while copied preflight output, tampered audit-record truth, and contaminated retry-worker claims are rejected without creating retry ledgers, retry workers, schedules, credential persistence, public hosting, default live delivery, background retry, automatic vendor retry, receipt persistence, or raw host-data persistence.
- `verify:phase208` locks append-only redacted retry-control work-item closure retry-ledger entry persistence: only trusted retry-ledger entry plans can append deterministic JSONL records with persisted-at/by provenance and existing retry-ledger id preservation, while copied plans, missing stores, append failures, and tampered journals reject without retry workers, schedules, credential persistence, public hosting, default live delivery, background retry, automatic vendor retry, receipt persistence, or raw host-data persistence.
- `verify:phase209` locks trusted supplied retry-control work-item closure retry-ledger entry preflight: only trusted persisted retry-control closure retry-ledger entry objects can pass downstream preflight, while copied entries, tampered entries, and append-failed pending entries reject without retry workers, schedules, credential persistence, public hosting, default live delivery, background retry, automatic vendor retry, receipt persistence, or raw host-data persistence.
- `verify:phase210` locks trusted preflight-bound retry-control work-item closure retry-ledger entry readiness planning: only trusted supplied retry-ledger entry preflight envelopes over trusted persisted entries can produce deterministic readiness-plan truth, while copied preflights, shallow-copy identity breaks, contaminated retry-worker claims, and nested mutation attempts reject without retry workers, schedules, credential persistence, public hosting, default live delivery, background retry, automatic vendor retry, receipt persistence, or raw host-data persistence.
- `verify:phase211` locks supplied retry-control work-item closure retry-ledger entry readiness-plan preflight: only trusted supplied readiness-plan objects whose current truth matches freshly recomputed trusted preflight-bound readiness-plan truth can pass downstream preflight, while copied, shallow-copied, tampered, contaminated, and nested mutation attempts reject without retry workers, schedules, credential persistence, public hosting, default live delivery, background retry, automatic vendor retry, receipt persistence, or raw host-data persistence.
- `verify:phase212` locks trusted readiness-plan-preflight-bound retry-control work-item closure retry-ledger entry operator handoff: only trusted, frozen supplied preflight envelopes can create deterministic host/operator handoff descriptors for later worker selection, while copied, shallow-copied, contaminated, and nested mutation attempts reject without echoing untrusted preflight bodies or creating retry workers, schedules, credential persistence, public hosting, default live delivery, background retry, automatic vendor retry, receipt persistence, or raw host-data persistence.
- `verify:phase213` locks trusted supplied retry-control work-item closure retry-ledger entry operator handoff preflight: only trusted handoff objects recomputed from current readiness-plan-preflight truth are accepted, while copied, shallow-copied, tampered, contaminated, and nested mutation attempts reject without echoing untrusted handoff bodies or creating retry workers, schedules, credential persistence, public hosting, default live delivery, background retry, automatic vendor retry, receipt persistence, or raw host-data persistence.
- `verify:phase214` locks trusted retry-control work-item closure retry-ledger entry worker-selection intent: only trusted operator-handoff preflights can create deterministic host-owned foreground manual-reinvoke worker-selection descriptors, while copied, shallow-copied, tampered, contaminated, unsupported-mode, and nested mutation attempts reject without echoing untrusted bodies or creating retry workers, schedules, credential persistence, public hosting, default live delivery, background retry, automatic vendor retry, receipt persistence, or raw host-data persistence.
- `verify:phase215` locks trusted retry-control work-item closure retry-ledger entry worker handler-readiness: only trusted worker-selection descriptors plus a branded host foreground manual-reinvoke handler can create deterministic handler-readiness descriptors, while copied, shallow-copied, contaminated, missing/unbranded-handler, and nested mutation attempts reject without echoing untrusted bodies or creating retry workers, schedules, credential persistence, public hosting, default live delivery, background retry, automatic vendor retry, receipt persistence, handler execution, or raw host-data persistence.
- `verify:phase216` locks trusted retry-control work-item closure retry-ledger entry worker invocation handoff: only trusted handler-readiness descriptors plus a branded capability-configured host foreground manual-reinvoke handler can create deterministic host-execution-required-but-not-attempted invocation handoffs, while copied, shallow-copied, contaminated, missing-handler, and nested mutation attempts reject without echoing untrusted bodies or creating retry workers, schedules, credential persistence, public hosting, default live delivery, background retry, automatic vendor retry, receipt persistence, handler execution, or raw host-data persistence.
- `verify:phase217` locks trusted supplied retry-control work-item closure retry-ledger entry worker invocation-handoff preflight: only trusted supplied invocation handoffs that match freshly recomputed current truth from trusted handler-readiness plus a branded capability-configured host foreground manual-reinvoke handler are accepted, while copied, shallow-copied, tampered, contaminated, missing-handoff, and nested mutation attempts reject without echoing untrusted bodies or creating retry workers, schedules, credential persistence, public hosting, default live delivery, background retry, automatic vendor retry, receipt persistence, handler execution, or raw host-data persistence.
- `verify:phase218` locks trusted retry-control work-item closure retry-ledger entry worker execution-plan descriptors: only trusted supplied Phase217 invocation-handoff preflights that match freshly recomputed current truth can produce deterministic frozen execution plans, while copied, tampered, contaminated, missing-preflight, and nested mutation attempts reject without echoing untrusted bodies or creating retry workers, schedules, credential persistence, public hosting, default live delivery, background retry, automatic vendor retry, receipt persistence, handler execution, or raw host-data persistence.
- `verify:phase219` locks trusted supplied retry-control work-item closure retry-ledger entry worker execution-plan preflights: only trusted supplied Phase218 execution-plan descriptors that match freshly recomputed current plan truth are accepted, while copied, tampered, contaminated, missing-plan, and nested mutation attempts reject without echoing untrusted bodies or creating retry workers, schedules, credential persistence, public hosting, default live delivery, background retry, automatic vendor retry, receipt persistence, handler execution, or raw host-data persistence.
- `verify:phase220` locks execution-plan-preflight-bound retry-control work-item closure retry-ledger entry worker foreground execution: only trusted supplied Phase219 execution-plan preflights that match freshly recomputed current plan-preflight truth can invoke one branded host-owned foreground worker handler, while copied, missing, contaminated, unbranded-handler, and nested mutation attempts reject before execution without creating retry workers, schedules, credential persistence, public hosting, default live delivery, background retry, automatic vendor retry, receipt persistence, or raw host-data persistence.
- `verify:phase221` locks deterministic redacted non-persistent retry-ledger entry worker execution receipts: only trusted Phase220 executions that match freshly recomputed current execution-plan preflight truth can produce a frozen receipt descriptor over sanitized host-result truth, while copied, tampered, failed, contaminated, and nested mutation attempts reject without creating retry workers, schedules, credential persistence, public hosting, default live delivery, background retry, automatic vendor retry, receipt persistence, or raw host-data persistence.
- `verify:phase222` locks trusted supplied retry-ledger entry worker execution-receipt preflights: only trusted supplied Phase221 receipts that match freshly recomputed current receipt truth from trusted Phase220 execution truth are accepted, while copied, tampered, missing, contaminated, and nested mutation attempts reject without creating retry workers, schedules, credential persistence, public hosting, default live delivery, background retry, automatic vendor retry, receipt persistence, or raw host-data persistence.
- `verify:phase223` locks trusted receipt-preflight-bound retry-ledger entry worker execution-receipt closeout readiness: only trusted supplied Phase222 preflights that match freshly recomputed current preflight truth are accepted, while copied, tampered, missing, contaminated, and nested mutation attempts reject without creating retry workers, schedules, credential persistence, public hosting, default live delivery, background retry, automatic vendor retry, receipt persistence, closeout persistence, or raw host-data persistence.
- `verify:phase224` locks trusted supplied retry-ledger entry worker execution-receipt closeout preflight truth: only trusted supplied Phase223 closeouts that match freshly recomputed current closeout truth are accepted, while copied, tampered, missing, contaminated, and nested mutation attempts reject without creating retry workers, schedules, credential persistence, public hosting, default live delivery, background retry, automatic vendor retry, receipt persistence, closeout persistence, or raw host-data persistence.
- `verify:phase225` locks trusted closeout-preflight-bound retry-ledger entry worker execution-receipt closeout record-plan truth: only trusted supplied Phase224 closeout preflights that match freshly recomputed current closeout-preflight truth are accepted, while copied, tampered, missing, contaminated, and nested mutation attempts reject without creating retry workers, schedules, credential persistence, public hosting, default live delivery, background retry, automatic vendor retry, receipt persistence, closeout persistence, durable closeout records, or raw host-data persistence.
- `verify:phase149` locks manual retry replay source revalidation planning for external media transfer: accepted source-stage and vendor-stage replay host actions can produce a fingerprint-only host/operator source revalidation plan with one fresh-resolution step per source fingerprint and vendor-state-check truth for vendor-stage retries, while stale approvals, tampered work items, and current truth mismatches produce no plan. The plan does not resolve sources, fetch/download/upload/preview files, persist revalidation or work items, create retry ledgers, durable retry audit records, retry workers, schedules, public hosting, credential persistence, automatic vendor retry, or default live delivery, and it carries no raw refs, raw target identifiers, approval signatures, credentials, URLs, or file bytes.
- `verify:phase148` locks manual retry replay host-action handoff for external media transfer: accepted source-stage and vendor-stage replay preflights can produce a credential-free host-owned manual reinvoke descriptor with transfer key, bounded source fingerprints, target fingerprint, correlation truth, and vendor-state-check truth, while stale approvals, tampered work items, and current truth mismatches produce no host action. The handoff does not execute a host handler, persist a work item, create retry ledgers, durable retry audit records, retry workers, schedules, public hosting, credential persistence, automatic vendor retry, or default live delivery, and it carries no raw refs, raw target identifiers, approval signatures, credentials, URLs, or file bytes.
- `verify:phase147` locks manual retry work-item replay preflight for external media transfer: a supplied work item is checked against a fresh approval-bound candidate by recomputing transfer key, source fingerprints, target fingerprint, and work-item correlation id; source-stage and vendor-stage preflights preserve fresh approval/source revalidation truth, vendor-stage requires vendor-state checks, stale approvals and source/target/tamper mismatches fail closed, and no host handler, retry worker, schedule, ledger, durable audit record, public hosting, credential persistence, or default live delivery is created. `verify:phase146` locks deterministic manual retry work-item correlation for retryable external media transfer failures: source-stage, vendor-stage, and generic host-handler work items carry a digest id derived from approved transfer key, retry stage, source fingerprints, and target fingerprint; same source work items stay stable across wait-hint changes, stage/target changes produce different ids, host-forged correlation/persistence/ledger claims are ignored and redacted, and no retry ledger or durable retry audit record is created. `verify:phase145` locks the bounded manual retry work-item descriptor for retryable external media transfer failures: source-stage, vendor-stage, and generic host-handler failures carry a recomputed operator-foreground-reinvoke work item from approved transfer truth, sanitized retry stage, source fingerprints, target fingerprint, and bounded wait hints; success/non-retryable paths emit no work item, and host-forged worker/schedule/credential/live-delivery/public-hosting claims plus raw refs, target ids, approval signatures, credentials, URLs, and bytes are redacted or ignored. `verify:phase144` locks request-only vendor-send target context for the opt-in external media transfer worker: host senders receive bounded target kind, deterministic approval-bound target fingerprint, request-only truth, and raw-target non-persistence truth; sender requests do not duplicate raw workspace/account/target/thread identifiers, durable success/failure data does not copy the target context, host-forged request-target fields in reasons are redacted, and source-stage failures create no sender request. `verify:phase143` locks manual retry target-correlation handoff truth for retryable external media transfer failures: source-stage and vendor-stage retryable failures carry a deterministic target fingerprint plus bounded target kind derived from the approval-bound action, same targets produce stable fingerprints, different targets produce different fingerprints, host-forged target claims are ignored, raw workspace/account/target/thread ids are not persisted, and success/non-retryable paths carry no retry target-correlation metadata. `verify:phase142` locks manual retry approval replay handoff truth for retryable external media transfer failures: source-stage and vendor-stage retryable failures require a fresh operator approval signature, previous approval signatures are not persisted or reusable for manual reinvoke, host-forged approval replay claims are ignored, and success/non-retryable paths carry no retry approval replay metadata. `verify:phase141` locks deterministic manual retry backoff correlation handoff for retryable external media transfer failures: transfer-key-bound correlation ids are derived from bounded retry-after state, host-forged correlation/timer/schedule/persistence claims are ignored, and no timer, schedule, automatic retry, durable audit record, raw refs, credentials, URLs, bytes, or request-only context is created. `verify:phase140` locks manual retry backoff hints for retryable external media transfer failures: bounded Retry-After truth becomes operator-only recommended wait metadata, generic retryable host rejections without Retry-After make no wait-time claim, and no retry worker, schedule, automatic retry, or durable backoff persistence is created. `verify:phase139` locks durable manual retry source-ref fingerprint handoff for retryable external media transfer failures: source-resolution, vendor-send, and generic retryable host rejections carry bounded approved source-ref fingerprints plus revalidation flags derived from the approval-bound host action without persisting raw refs, content hashes, request-only context, credentials, URLs, file bytes, or host-injected fingerprint claims. `verify:phase138` locks sanitized stage-derived next operator actions for retryable external media transfer failures: source-resolution retries instruct source-ref revalidation before any vendor send and state that no vendor-state check is required, while vendor-send retries instruct vendor-state verification before reinvoke and only then fresh source-ref revalidation. Host-supplied action text is ignored and recomputed from the allowlisted retry stage; generic retryable host rejections without stage safety keep only the generic next operator action. `verify:phase137` locks sanitized stage-derived manual retry reason codes for retryable external media transfer failures: source-resolution retries use `source_resolution_retryable_failure`, vendor-send retries use `vendor_send_retryable_failure`, host-supplied reason-code claims are ignored and recomputed from the allowlisted retry stage, and generic retryable host rejections without stage safety keep only the generic retry reason. `verify:phase136` locks stage-specific manual retry verification scope for retryable external media transfer failures: source-resolution retries name source-ref revalidation before vendor send and do not require vendor-state checks, while vendor-send retries require vendor-state verification before fresh source-ref revalidation; host-supplied scope claims are recomputed from the allowlisted retry stage.
- `verify:phase135` locks durable manual retry revalidation handoff truth for retryable external media transfer failures: retryable host rejections, source timeouts, and vendor timeouts require a fresh approval check, source-ref revalidation, fresh source resolution, and no stale resolved-file reuse before operator reinvoke while preserving manual-only retry behavior. `verify:phase134` locks sanitized manual retry safety context for external media transfer failures: retryable source-resolution failures state that no vendor send happened, while retryable vendor-send failures/timeouts require operator vendor-state verification before manual reinvoke and still forbid automatic vendor retry. `verify:phase133` locks request-only vendor-send attempt context for external media transfer foreground sends: host senders always receive transfer key, channel id, resolved-file count, single-attempt cap, retry-attempt zero truth, no-automatic-retry policy, timeout enablement truth, and timeout abort signals only when configured, without copying that volatile context into durable result data. `verify:phase132` locks sanitized per-file source retry outcome metadata for external media transfer foreground retries: retried files can report safe source-ref fingerprints, bounded file indexes, recovered/exhausted status, per-file retry counts, last failure kind, and bounded retry-after seconds without copying raw refs, free-form reasons, URLs, credentials, bytes, approval signatures, or request-only previous-failure fields into durable result data. `verify:phase131` locks request-only previous source-failure context for the external media transfer worker: retrying resolver requests carry previous failure kind, retryable truth, and bounded retry-after seconds, reset per file, use fresh timeout abort signals, and do not copy raw reasons, refs, URLs, credentials, bytes, approval signatures, or the volatile previous-failure fields into durable result data. `verify:phase130` locks request-only source attempt context for the external media transfer worker: resolver requests carry one-based attempt number, per-file max-attempt cap, retry-attempt count, and retry truth, reset per file, and do not copy that volatile context into durable result data. `verify:phase129` locks sanitized foreground source-retry metadata for external media transfer host results: recovered/exhausted source retries are copied only as bounded host-reported retry metadata, unsafe handler fields and free-form reasons are dropped, vendor failures after source recovery remain manual-only, and no retry worker/schedule, default live delivery, public hosting, credential persistence, raw refs, URLs, bytes, or approval signatures are created or persisted. `verify:phase128` locks opt-in source-resolution foreground retry for the external media transfer worker: retryable host source failures and source timeouts can retry once before vendor send with fresh abort signals, exhaustion surfaces existing manual retry metadata, non-retryable/validation/vendor-send failures are not retried, and no retry worker/schedule, default live delivery, public hosting, credential persistence, or Colony-owned file transfer is created. `verify:phase127` locks opt-in vendor-send timeout handling for the external media transfer worker: configured host vendor send receives a cooperative abort signal after source resolution, timeout failures surface existing manual retry metadata, late sender success/receipt metadata is not copied, and no retry worker/schedule, default live delivery, public hosting, credential persistence, or Colony-owned file transfer is created. `verify:phase126` locks external subscription credential setup readiness UX: Slack and Discord setup plans expose redacted required credential-ref labels, present/missing/invalid ref status, host-supplied runtime secret/config responsibilities, and host-only handoff checklist rendering without raw refs, tokens, callback URLs, credential persistence, public hosting, or default live delivery claims. `verify:phase125` locks opt-in source-resolution timeout handling for the external media transfer worker: configured source resolution receives a cooperative abort signal, timeout failures reject before vendor send, surface existing manual retry metadata, and create no retry worker/schedule, default live delivery, public hosting, credential persistence, or Colony-owned file transfer. `verify:phase124` locks the opt-in external media transfer host worker factory: the worker is inert unless explicitly supplied as the media transfer handler, requires injected source resolution and vendor send capabilities, gives resolvers only safe refs plus fingerprints, passes senders resolved file descriptors without raw source refs, rejects oversized files, checksum mismatches, and forbidden resolver payloads, preserves existing transfer-key integrity, keeps retry UX manual-only, and creates no default live delivery, public hosting, credential persistence, or retry worker. `verify:phase123` locks host-reported external media transfer success receipt metadata: receipts are copied only after transfer-key-bound success, receipt ids/status/counts are bounded and redacted, receipt URLs are not persisted, and receipt truth explicitly does not claim Colony-verified delivery or file inspection. `verify:phase122` locks manual operator retry UX for explicit retryable host-owned external media transfer rejections: retryable host results expose bounded manual reinvoke metadata, clamp retry-after seconds, create no retry worker or schedule, perform no automatic second attempt, and preserve non-retryable unclassified handler exceptions plus transfer-key mismatch integrity failures. `verify:phase121` locks transfer-key-bound external media transfer host results: accepted host handler results must echo the current transfer key before success is reported; missing or wrong echoes fail closed without a success claim or copied vendor message id; rejected handlers remain ordinary rejections; and unsafe vendor message ids are redacted as a whole after a matching echo. `verify:phase120` locks stable external media transfer handoff keys and source-ref fingerprints for host idempotency/audit: keys are domain-separated, deterministic, bind the full normalized approval candidate including files beyond the five-file host-action cap, and do not replace exact approval signatures or claim Colony-verified content/checksum truth. `verify:phase119` locks approval-bound external media transfer handoff: exact signatures bind safe host-owned media refs and bounded metadata, unsafe URLs/paths/inline bytes/private fields fail closed, stale approvals invalidate before handler execution, missing/ambiguous host inputs reject, and successful host actions still do not claim default live delivery, public hosting, credential persistence, adapter attachment delivery, retry workers, or vendor upload/download.
- `verify:phase117` locks a redacted durable approved Slack event binding gate: approved host-owned Slack setup can append a binding record with workspace, event type, callback fingerprint, signing-secret reference redaction, and approval-signature fingerprint, while signed and host-authenticated Slack callbacks fail closed before bridge acceptance unless a supplied binding reader/list matches workspace and event type. Malformed binding journals fail closed, and the slice stores no raw tokens, manifests, signing secrets, callback URLs, approval signatures, or event bodies; it still creates no default listener, public hosting, upload/media path, retry worker, or default live inbound delivery.
- `verify:phase116` locks duplicate Slack event retry dedupe at the channel session bridge route boundary: repeated signed and host-authenticated Slack `message` or `app_mention` callbacks with the same normalized inbound message id receive a bounded 202 duplicate ACK, do not increment route message count, do not dispatch the session runner again, and do not send a second adapter reply, without credential, listener, public-hosting, upload, retry-worker, persistence, or default-live-delivery claims.
- `verify:phase115` locks Slack `app_mention` as an approval-bound alternative Events API subscription and inbound event shape: mention-only setup requires `app_mentions:read`, keeps mixed Slack event subscriptions rejected, patches only `app_mention` bot events, and signed plus host-authenticated mention callbacks use the existing deferred ACK bridge path without credential, listener, public-hosting, upload, retry-worker, or default-live-delivery claims.
- `verify:phase114` locks Slack `event_callback` deferred ACK: signed and host-authenticated Slack message events are accepted into the existing channel/session bridge and return 202 before runner/reply completion, no adapter reply is emitted before ACK, asynchronous replies preserve Slack thread routing, and signature/auth failures plus `url_verification` remain fail-closed or handshake-only without credential, listener, public-hosting, upload, retry-worker, or default-live-delivery claims. `verify:phase113` locks Slack subscription activation readiness: approved host-owned Slack setup preflights the full manifest for the required `channels:history` bot scope before fetch, renders scope readiness in `/channels external`, and reports success-only activation-readiness plus remaining operator steps after `apps.manifest.update`, without claiming live delivery, public hosting, credential persistence, listener startup, uploads/media, retry workers, or schedules. `verify:phase112` locks signed Discord `APPLICATION_COMMAND` inbound dispatch: verified non-PING command interactions require host webhook/inbound auth, dispatch through the existing channel/session bridge, return only `{ "type": 5 }` after bridge acceptance, and preserve PING/message regressions without credential, interaction-token, listener, public-hosting, upload, privileged-intent, or default-live-delivery claims. `verify:phase111` locks Discord command response definition integrity for host-owned guild command registration: successful Discord API responses must echo each approved command name, description, and chat-input type before mutation success is reported; mismatches are non-retryable, redact secrets, and do not report command mutation success or retry worker/schedule metadata. `verify:phase110` locks optional Slack manifest echo integrity for host-owned `apps.manifest.update`: when Slack returns a manifest echo, its Events API request URL and bot events must match the approved patched manifest; mismatches are non-retryable, redact secrets, and do not report mutation success or retry worker/schedule metadata, while omitted manifest echoes remain compatible. `verify:phase109` locks Slack response identity binding for host-owned `apps.manifest.update`: successful Slack responses must include the approved `app_id`; optional `team_id`, `workspace_id`, or `team` values fail closed when they do not match the approved workspace; identity mismatches are non-retryable, redact secrets, and do not report mutation success or retry worker/schedule metadata. `verify:phase108` locks Slack subscription setup handoff UX: `/channels external` renders a redacted host handoff checklist, default `manual_operator_reinvoke` retry mode, optional `host_inline_bounded` foreground retry mode, max-two foreground attempts, planned host manifest-submission wording, and no submitted-before-execution overclaim; `/channels external subscribe slack <approval-signature>` preserves the credential-free action shape and creates no retry worker/schedule, credential persistence, public hosting, or default live delivery. `verify:phase107` locks host-owned Discord guild application-command registration: approved private candidates can bulk-overwrite one to five approval-bound chat-input guild commands through one injected Discord API call, validate command definitions before fetch, reject post-approval command mutation, verify returned command names, and Phase111 extends response integrity to descriptions and type; preserve the Phase 104 endpoint mutation as the default Discord setup mode, and still create no Discord apps, credential persistence, endpoint mutation in command mode, listener startup, public hosting, upload/media path, retry worker, privileged Gateway intents, or default live delivery. `verify:phase106` locks opt-in bounded foreground Slack retry: approved host-owned Slack setup can retry one retryable fetch/429/5xx failure once inside the same injected host call, reports `bounded_foreground_retry` attempt metadata on recovery or exhaustion, clamps requested attempts to two, preserves Phase 105 manual-only default behavior, blocks fetch before approval, skips retry for non-retryable Slack failures, leaves Discord/gateway action shape unchanged, and still creates no background worker, retry schedule, credential persistence, manifest persistence, approval-signature persistence, upload/media path, public hosting, or default live delivery. `verify:phase105` locks Slack subscription manual retry UX metadata: retryable Slack setup failures expose redacted `manual_operator_reinvoke` guidance, bounded `Retry-After` seconds, and stable retry reasons without any automatic retry, worker, schedule, credential persistence, manifest persistence, approval-signature persistence, Discord regression, or default live delivery. `verify:phase104` locks host-owned Discord Interactions endpoint mutation execution: approved private candidates with an approval-bound bot token perform one injected Discord Edit Current Application `PATCH /applications/@me` call with body `{ interactions_endpoint_url }`, verify returned application id and endpoint URL, fail closed on missing/ambiguous/pending/mutated host inputs and malformed/oversized/API responses, classify retryability without retry workers, redact token/API failures, and still do not create Discord apps, register slash commands, store credential values, start listeners, provide public hosting, upload media, enable privileged Gateway intents, mutate Slack setup, or enable default live inbound delivery. `verify:phase103` locks signed Discord Interactions PING/PONG readiness in the host-owned external webhook transport: injected vendor signature verification is required before parsing, `type: 1` returns only `{ "type": 1 }`, the handshake bypasses Colony host auth and session-bridge dispatch only for that PING, and malformed/rejected requests fail closed with redaction. `verify:phase102` locks the host-owned Slack subscription direct mutation executor: one injected Slack `apps.manifest.update` call for an approved private candidate with host-supplied full manifest/token, redacted fail-closed host inputs and Slack API failures, retryable classification without retry workers, and no Slack app creation, credential persistence, listener startup, public hosting, uploads, Discord-side setup, or default live inbound delivery. `verify:phase101` locks approval-gated Discord Interactions setup request staging: exact approval binds Discord application/guild/callback/public-key-ref/enabled intent/`PING` and `APPLICATION_COMMAND`, accepted `/channels external subscribe discord <approval-signature>` emits a credential-free host action, and the gateway does not create Discord apps, register interaction endpoints or slash commands directly, store credential values, start listeners, provide public hosting, upload media, run retries, enable privileged Gateway intents, or enable default live inbound delivery. `verify:phase100` locks approval-gated Slack subscription setup planning and gateway request staging: exact approval binds Slack app/workspace/callback/signing-secret-ref/enabled intent/`message.channels`, accepted `/channels external subscribe slack <approval-signature>` emits a credential-free host action, and the gateway does not create Slack apps, register subscriptions directly, store credential values, start listeners, provide public hosting, upload media, run retries, perform direct Discord API/app/endpoint setup, or enable default live inbound delivery. `verify:phase99` locks signed Slack Events API `url_verification` challenge handling after vendor signature verification, returning only the Slack challenge without Colony host auth or session bridge dispatch for that handshake, while normal Slack `event_callback` delivery still requires host auth and bridge execution. `verify:phase98` locks Telegram-only host-owned webhook setup with exact approval signatures, credential-free `/channels external webhook telegram <approval-signature>` host actions, one injected Telegram `setWebhook` call, bounded redacted Telegram API failure handling, and real `X-Telegram-Bot-Api-Secret-Token` host-auth compatibility without listener startup, credential persistence, auth mutation, adapter registration, public hosting, uploads, retry workers, Slack setup or direct Discord API/app/endpoint setup. `verify:phase97` locks host-owned external vendor webhook transport with injected signature verification, host auth proof, redacted HTTP responses, and local listener smoke coverage without vendor webhook registration/subscription setup, credential setup, upload, retry worker, or default public hosting. `verify:phase96` locks host-owned external vendor event dispatch through an injected session bridge and host auth policy, with Slack/Discord thread routing, Telegram topic routing, runner-failure inspection, credential/text redaction, and no listener/subscription/credential/webhook/upload/retry setup by the dispatcher; reply delivery remains delegated to the injected bridge/registry. `verify:phase95` locks host-owned execution of accepted external registration actions without credential/signature leakage, vendor API calls, listener startup, or subscription setup. `verify:phase94` locks the credential-free `/channels external register <channel> <approval-signature>` host-action boundary for already accepted plans. `verify:phase93` locks `/channels external` operator visibility and pure Slack/Discord/Telegram event normalization helpers without default live adapter registration, credentials, vendor subscriptions, uploads, retries, or live inbound vendor delivery. `verify:phase92` locks explicit external adapter registration/config gates and vendor inbound signature helpers: exact approval signatures before Slack/Discord/Telegram registration, explicit enabled intent, safe API base validation, raw-body HTTP signature verification hook, Slack HMAC, Telegram secret-token, and Discord fail-closed injected-verifier semantics.
- `verify:phase90` locks the local-first GitHub PR handoff workflow: dry-run issue intake, deterministic branch/worktree naming, credential redaction, approval-gated local workspace creation, implementation and verification ordering, failed-verification blocking before PR handoff, second approval before PR creation handoff, and daemon workflow-template exposure.
- `ChannelRegistry`, `InMemoryChannelAdapter`, deterministic channel route keys, and `/channels` status/deliveries views provide the first channel surface foundation.
- `verify:phase32` locks route keys, delivery records, missing/disabled channel fail-closed behavior, parser truth, and operator visibility.
- `ChannelAuthPolicy`, `ChannelPairingStore`, normalized inbound webhook envelopes, and `/channels auth` provide the first redacted channel auth/pairing foundation.
- `verify:phase33` locks webhook secret checks, pairing-gated inbound authorization, inbound route normalization, parser truth, and auth operator visibility.
- `handleChannelWebhookRequest` and `ChannelWebhookHttpServer` provide HTTP channel webhook validation and a real local listener path for normalized inbound delivery.
- `verify:phase34` locks request validation, authenticated inbound delivery callbacks, secret redaction, and local listener behavior.
- `ChannelSessionBridge` maps inbound channel route keys to stable session ids and bridges runner replies back through `ChannelRegistry`.
- `verify:phase35` locks route reuse, route isolation, reply delivery records, disabled-channel failures, and runner failure visibility.
- `/channels sessions` exposes channel session bridge route and recent-turn status to operators.
- `verify:phase36` locks channel-session operator visibility, empty-state truth, and secret non-leakage.
- `verify:phase88` locks contract-only Slack/Discord/Telegram-style channel fixtures, route-key previews, threading/mention/reaction/attachment/retry/auth/redaction semantics, fail-closed malformed or secret-bearing fixture validation, proof that fixtures do not register send adapters, and `/channels contracts` visibility without claiming real vendor delivery.
- `verify:phase37` locks `/channels` help text truth across direct help formatting and parser-backed `/help`.
- `verify:phase38` locks `/permissions` help text truth across direct help formatting and parser-backed `/help`.
- `verify:phase39` locks `/provider` help text truth across direct help formatting and parser-backed `/help`.
- `verify:phase40` locks `/skills` help text truth across direct help formatting and parser-backed `/help`.
- `verify:phase41` locks `/doctor` help text truth across direct help formatting and parser-backed `/help`.
- `verify:phase42` locks `/workspace` help text truth across direct help formatting and parser-backed `/help`.
- `verify:phase43` locks `/hooks` help text truth across direct help formatting and parser-backed `/help`.
- `verify:phase44` locks `/events` help text truth across direct help formatting and parser-backed `/help`.
- `verify:phase45` locks `/perf` help text truth across direct help formatting and parser-backed `/help`.
- `verify:phase46` locks `/tools` help text truth across direct help formatting and parser-backed `/help`.
- `verify:phase47` locks `/cost` help text truth across direct help formatting and parser-backed `/help`.
- `verify:phase48` locks `/budget` help text truth across direct help formatting and parser-backed `/help`.
- `verify:phase49` locks `/model` help text truth across direct help formatting and parser-backed `/help`.
- `verify:phase50` locks `/status` help text truth across direct help formatting and parser-backed `/help`.
- `verify:phase51` locks `/artifact` help text truth across direct help formatting and parser-backed `/help`.
- `verify:phase52` locks `/compact` help text truth across direct help formatting and parser-backed `/help`.
- `verify:phase53` locks skill catalog audit classification, alias detection, metadata/body gap reporting, and `/skills audit` operator visibility.
- `verify:phase54` locks skill source metadata loading, source drift counts, stale revision reporting, and body-safe `/skills audit` source rendering.
- `verify:phase55` locks safe skill import/update planning, dry-run action counts, approval-boundary rendering, and body-safe `/skills plan` output.
- `verify:phase56` locks explicit-approval skill staging, quarantine-only writes, body-free manifests, live catalog isolation, and staged audit gates.
- `verify:phase57` locks staged-skill promotion, second-approval enforcement, audit-gated live writes, rollback preservation, and body-free promotion manifests.
- `verify:phase58` locks staged-skill operator views for preview, audit, approve/promote status, rollback evidence, and body-safe rendering.
- `verify:phase59` locks approved staged-skill rollback execution, missing-evidence rejection, live restoration, and body-free rollback metadata.
- `verify:phase60` locks approved rollback-result rendering, fail-closed missing-result handling, and body-safe `/skills staged rollback --approved` output.
- `verify:phase61` locks staged skill lifecycle event projection and body-safe `/skills staged history` inspection for stage/promote/rollback events.
- web control foundation exists through `handleWebControlRequest` and `serializeWebControlState`; the local operator shell renders provider/workflow/swarm/channel status, local scoped mutation handoff controls require explicit opt-in, local host binding, `web.mutate`, and approval, while hosted/default-public browser delivery remains open.
- local-first GitHub PR handoff workflow foundation exists through `createGitHubPrHandoffWorkflow`, `createGitHubLocalWorkspaceActionPlan`, `executeApprovedGitHubLocalWorkspaceAction`, `createGitHubVerificationToPrHandoff`, `createGitHubVerificationToPrHandoffWorkflowHandler`, and the `github_pr_handoff` workflow template; verified local execution can produce a redacted PR handoff artifact, while real GitHub issue fetch, push, and PR creation remain host-executed future work behind explicit approvals.
- deeper durable session recovery integration, broader remote action controls, and remaining default/live vendor subscription and delivery semantics after the shipped Phase 113-225 host-owned foundations: broader real host upload/download/retry worker execution beyond the opt-in foreground factory and manual retry closeout/closure/audit/retry-ledger entry persistence, preflight, readiness-planning, operator-handoff, operator-handoff-preflight, worker-selection intent, handler-readiness, invocation-handoff, invocation-handoff-preflight, worker execution-plan context, execution-plan-preflight context, trusted foreground worker execution, non-persistent worker execution receipt, supplied execution-receipt preflight, receipt-preflight-bound closeout readiness, supplied closeout-preflight, and closeout-preflight-bound record-plan boundaries, broader Discord live-delivery UX, default live delivery, and channel-specific delivery runtime semantics remain open.

Exit criteria:

- Same runtime can serve terminal, headless, and remote clients.

Estimated effort: 3-4 weeks

## Phase 7 - Hardening, Packaging, and Release

Goal: Ship release candidate, not demo.

Focus:

- Security hardening gaps.
- Failure-injection and recovery testing.
- Performance and token benchmarks.
- Packaging, first-run experience, diagnostics, operator docs.
- Antelligence launch narrative, launch checklist, and enterprise template readiness.

Exit criteria:

- Release candidate with repeatable verification, clear operator story, Antelligence launch framing, and known risk envelope.

Estimated effort: 2 weeks

## Immediate Active Slices

This is current execution queue:

1. Phase 2 memory polish only when concrete retrieval precision or inspection gaps are discovered.
2. Release-grade Alpha 0 hardening around source+Bun onboarding, claim safety, build packaging, and the real LLM swarm demo.
3. Richer swarm visualization and production hardening beyond the Launch Alpha 0 real planner/worker/reviewer execution path.
4. Remote action-control and workflow automation hardening beyond the daemon/client/control-plane foundation.
5. Phase 2 / Alpha 3 memory polish only when new concrete recall precision, transcript truth, inspection, or recovery gaps are discovered.
6. Phase 7 end-to-end hardening and release-gate packaging.

## Sequencing Rules

- Do not chase broad tool parity before memory and workflow foundations exist and are wired into runtime surfaces.
- Do not expand multi-surface gateway work beyond shipped daemon/channel foundations without preserving terminal runtime truth.
- Do not treat Python parity as finish line where Python itself was unfinished.
- Every slice must land with verification and doc updates.
