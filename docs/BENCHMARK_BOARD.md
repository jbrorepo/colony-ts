# The Colony - Benchmark Board

Last Updated: 2026-05-14

This board tracks capability-class parity against Claude Code and OpenClaw.
For the broader market comparison against Codex, Copilot, Cursor, Devin,
Gemini CLI, Aider, Continue, Windsurf, Junie, Cody, and related benchmark
patterns, see `MARKET_GAP_ANALYSIS.md`.
For the latest full Claude Code/OpenClaw comparison and closure plan, see
`docs/release/COMPETITOR_GAP_ANALYSIS_2026-05-11.md`.

Decision locked: parity means matching or exceeding each major capability class
with Colony-native implementations first. Long-tail adapters and commands are
post-RC backlog unless they are required to complete a capability class.

## Non-Negotiables

- Keep Colony branding and the 12-caste method framework, with legacy runtime caste values accepted only as compatibility aliases.
- Keep MemPalace as the long-term memory architecture.
- Keep exact transcript truth separate from derived compact artifacts.
- Keep Caveman compression for model-bound and internal agent traffic, never as canonical storage.
- Keep security and approval defaults stricter than the benchmarks where they differ.

## Capability Board

| Capability class | Colony now | Claude Code | OpenClaw | Main gap | Planned phase |
| --- | --- | --- | --- | --- | --- |
| Single-agent terminal core | Strong | Strong | Medium | command honesty, UI depth, compat, perf, operator polish | Phase 1 |
| Memory and recall | Partial + query-plan/structured-preview + ownership recall visibility | Medium | Medium | deeper extraction breadth, broader operator surfacing, and final MemPalace completion after major routing/ranking progress | Phase 2 |
| Workflow and tasks | Foundation + visibility + step policy + runtime runner + AgentLoop adapter + packaged templates + automation control | Strong | Medium | remote approval operator UX, broader task automation, failure injection, and release-grade resumability | Phase 3 + 6 |
| Multi-agent colony runtime | Spawn/lifecycle + coordinator queue/messaging + fan-out/fan-in/cancellation + policy propagation + `/swarm` coordinator-only start/status/cancel + LLM-backed planner/worker/reviewer execution + durable restart-safe status/resume/retry snapshots + stage timeline/retry-history/artifact-review/interrupted-resume/approval-wait inspection | Strong | Strong | long-run production hardening | Phase 4 |
| Tools, skills, MCP, plugins | Skills catalog + staged lifecycle visibility + MCP foundation + approval-proof hardening + transport seam + guarded boundary + MCP resource list/read + stdio transport + trusted stdio config + trusted stdio operator/audit inspection + internal HTTP transport + trusted HTTP config/operator exposure + trusted plugin sidecar config + managed plugin sidecar lifecycle + plugin sidecar supervisor foundation + dry-run plugin package discovery/import planner + durable redacted package plan event journal + approval-gated plugin package catalog staging/live-disabled promotion + approval-gated plugin package install/update execution receipts through an injected executor + supplied registry metadata checksum/signature enrichment + approval-gated package-driven sidecar activation controls through an injected supervisor + approval-gated registry fetch host/network handoff boundary + injected host-executor registry fetch execution receipts + exact-approval package-code execution policy preflights + approved package-code execution receipts through an injected host executor + read-only built-in plugin marketplace view, marketplace registry metadata fetch handoff descriptors, registry fetch execution status, approved registry fetch handoff execution bridge, registry-fetch metadata planning, metadata-bound install/update handoff descriptors, approved metadata-bound install/update handoff execution receipts, metadata-bound install/update execution status, metadata-bound activation readiness, metadata-bound activation handoff descriptors, approved metadata-bound activation handoff execution receipts, metadata-bound activation execution status, marketplace lifecycle status, marketplace lifecycle handoff, marketplace lifecycle runbook, marketplace lifecycle approval packets, marketplace lifecycle approval review, install/update handoff descriptors, install/update execution status, approved install/update handoff execution bridge, activation readiness, activation handoff descriptors, approved activation handoff execution receipts, and read-only execution status projections over bundled descriptors + safe local `glob_find`/git/test/lint tool breadth + approval-gated `web_fetch`/`web_search` | Strong | Medium | built-in live/default registry fetch/install UX beyond injected handoff execution/status/views/planning, default/live plugin activation UX beyond explicit approved handoff execution, and production plugin fabric beyond approved receipts | Phase 5 |
| Daemon, remote, web delivery | Control-plane API + HTTP transport/client + typed remote actions + scoped auth + `/daemon` visibility + local-only authenticated web-control operator shell + local scoped web mutation handoff controls + local-first GitHub PR handoff workflow template + deterministic provided GitHub issue intake + approval-gated local GitHub branch/worktree action plan + injected approved local branch/worktree execution receipts + verified PR handoff artifact generation | Strong | Strong | deeper session recovery integration, broader remote action controls, hosted/default-public web delivery, remote push/PR creation only behind explicit approval and credentials | Phase 6 |
| Channels and voice | Channel skeleton through signed Slack/Discord/Telegram-style host-owned foundations through Phase 225 trusted closeout-preflight-bound closure retry-ledger entry worker execution-receipt closeout record-plan truth; all remain host-owned and non-default-live unless explicitly supplied by the host | Medium | Strong | remaining channel/live-delivery depth after the shipped Phase 113-225 host-owned foundations: broader real host upload/download/retry worker depth beyond the opt-in foreground factory and manual retry closeout/closure/audit/retry-ledger entry persistence, preflight, readiness-planning, operator-handoff, operator-handoff-preflight, worker-selection intent, handler-readiness, invocation-handoff, invocation-handoff-preflight, worker execution-plan context, execution-plan-preflight context, trusted foreground worker execution, non-persistent worker execution receipt, supplied execution-receipt preflight, receipt-preflight-bound closeout readiness, supplied closeout-preflight, and closeout-preflight-bound record-plan boundaries, default public hosting, default live inbound delivery, privileged Gateway intents, broader Discord live-delivery UX beyond Phase112 `APPLICATION_COMMAND` dispatch and the Phase 107 guild command executor, broader slash-command lifecycle/UX, and one voice surface | Phase 6 |
| Release and operator truth | Partial | Strong | Strong | docs truth, onboarding, packaging, security and performance release gates | Phase 0 + 7 |

Phase 225 update for the channel row: trusted supplied retry-control work-item closure retry-ledger entry worker execution-receipt closeout preflights can now produce deterministic closeout record-plan truth before any downstream persistence boundary. The record plan binds trusted supplied Phase224 closeout-preflight truth, Phase223 closeout truth, Phase222 preflight truth, Phase221 receipt truth, Phase220 execution truth, execution-plan/preflight truth, invocation-handoff id, handler-readiness and worker-selection ids, channel/target/retry-stage context, transfer key, source fingerprints, bounded delivered-count truth, transfer-key echo truth, host receipt-metadata presence, and no raw host receipt persistence. Copied, tampered, missing, contaminated, or nested-mutation attempts fail closed without echoing untrusted bodies. It still creates no retry worker, retry schedule, credential persistence, public hosting, default live delivery, background retry, automatic vendor retry, receipt persistence, closeout persistence, durable closeout record, default listener, or raw host-data persistence.

## Immediate Truth-Sync Closures

These must stay true before broader parity work continues:

- `verify:all` includes `verify:phase384` plus `tsc --noEmit`; `verify:alpha0` covers the launch-critical alpha path.
- Phase 279 through Phase 282 lock the 12-caste method framework compatibility rollout: legacy Python caste values remain accepted for persisted runtime state, while operator-facing, source-of-truth, runtime identity, and prompt-template display prefer Queen, Eldest, Assist-Ant, Command-ant, Vigil-ant, Develop-ant, Logist-ant, Consult-ant, Inform-ant, Cogniz-ant, Account-ant, and Oper-ant.
- `/swarm` is described as a real planner/worker/reviewer coordinator path with coordinator-only and LLM-backed execution modes, status/resume/retry/cancel actions, and durable restart-safe snapshot truth.
- Built-in tool docs and comments do not claim `http_request`, a built-in live Colony registry/package install client, default live plugin activation, default-registered Slack/Discord/Telegram adapters, live credentials, live inbound vendor delivery, vendor subscriptions, uploads, or voice until they are actually shipped. Current package execution truth is limited to approval-gated install/update receipts through an injected executor with lifecycle scripts disabled, supplied registry metadata enrichment, explicit sidecar activation through an injected supervisor only after a matching completed install/update receipt plus exact approval, a redacted approval-gated host handoff for registry metadata fetch, an exact-approval injected host-executor fetch receipt that performs no built-in Colony network execution, no-executor package-code policy preflights, approved package-code execution receipts that call only an injected host executor after a ready preflight, marketplace registry metadata fetch handoff descriptors that delegate only to the existing injected registry-fetch helper, read-only marketplace registry fetch execution status over bundled descriptors and supplied receipts, approved marketplace registry fetch handoff execution that delegates only to the existing injected registry-fetch helper, read-only marketplace registry-fetch metadata planning that requires a completed receipt plus host-supplied full metadata before passing metadata to the package planner, metadata-bound marketplace install/update handoff descriptors that require that metadata-ready planning gate before producing a usable existing install/update handoff, approved metadata-bound marketplace install/update handoff execution receipts that delegate only after the metadata-bound gate and nested handoff are ready, read-only metadata-bound install/update execution status over Phase 253 handoffs and Phase 254 receipts, read-only metadata-bound activation readiness over completed metadata-bound install status, redacted metadata-bound activation handoff descriptors over ready metadata-bound readiness entries, approved metadata-bound activation handoff execution receipts that delegate only after matching handoff/action/install-receipt/approval validation, read-only metadata-bound activation execution status over Phase 256 readiness and Phase 258 receipts, read-only marketplace lifecycle status over metadata planning/install/activation status views, read-only marketplace lifecycle handoff over lifecycle states into operator-only approval/inspection checklists, read-only marketplace lifecycle runbook over lifecycle handoffs into ordered operator/reviewer steps, read-only marketplace lifecycle approval packets over approval-required runbook steps, read-only marketplace lifecycle approval review over supplied approval evidence, read-only marketplace install/update handoff descriptors and install/update execution status over bundled descriptors and supplied receipts, approved marketplace install/update handoff execution that delegates only to the existing injected install/update helper, read-only marketplace/activation-readiness/activation-handoff views over bundled descriptors, approved marketplace activation handoff execution receipts, and read-only activation execution status views that perform no fetch/package-code execution/sidecar start/catalog mutation/credential persistence.
- Phase 266 adds read-only marketplace lifecycle approval handoff preflight descriptors over Phase 265 handoff output; the preflight view remains operator-only, redacted, filterable by state/cap, maps ready entries to bounded host-handoff metadata, and explicitly denies host actions.
- Phase 267 adds read-only marketplace lifecycle host handoff request descriptors over Phase 266 preflight output; the request view remains operator-only, redacted, filterable by state/kind/cap, maps ready entries to explicit descriptor-only host request commands, preserves approval blockers, and explicitly denies host actions.
- Phase 268 adds read-only marketplace lifecycle operator queues over Phase 267 host handoff requests; the queue view remains operator-only, redacted, filterable by lane/state/cap, groups approval blockers and presentable metadata/install/update/activation request lanes, and explicitly denies default live execution.
- Phase 269 adds read-only marketplace lifecycle default UX plans over Phase 268 operator queues; the prompt plan remains operator-only, redacted, filterable by command kind/state/cap, maps ready queue items to stable default prompt command labels/ids, preserves approval blockers, and explicitly denies default live execution.
- Phase 270 adds read-only marketplace lifecycle default UX command palettes over Phase 269 prompt plans; the palette remains operator-only, redacted, filterable by group/state/cap, maps ready prompt plans to host handoff command entries, preserves approval blockers as remediation entries, and explicitly denies default live execution.
- Phase 271 adds read-only marketplace lifecycle default UX command briefs over Phase 270 command palettes; the brief remains operator-only, redacted, filterable by section/state/cap, maps ready command entries to copy-safe request-bound lines, preserves approval blockers as remediation lines, and explicitly denies default live execution.
- Phase 272 adds read-only marketplace lifecycle default UX command transcripts over Phase 271 command briefs; the transcript remains operator-only, redacted, filterable by section/state/cap, maps brief lines to bounded terminal transcript lines and fixed safety summaries, preserves approval blockers as remediation lines, and explicitly denies default live execution.
- Phase 273 adds read-only marketplace lifecycle default UX command panels over Phase 272 command transcripts; the panel remains operator-only, redacted, filterable by lane/state/cap, maps transcript lines to ready-command and approval-remediation cards plus fixed safety summaries, preserves copy text and blockers, and explicitly denies default live execution.
- Phase 274 adds read-only marketplace lifecycle default UX command panel digests over Phase 273 command panels; the digest remains operator-only, redacted, filterable by group/state/cap, maps panel cards to concise ready-command and approval-remediation digest items plus fixed safety summaries, preserves copy text and blockers, and explicitly denies default live execution.
- Phase 275 adds read-only marketplace lifecycle default UX command panel digest clipboard packets over Phase 274 digests; the clipboard packet remains operator-only, redacted, filterable by section/state/cap, maps digest items to bounded ready-command and approval-remediation clipboard items plus fixed safety summaries and clipboard text, preserves copy text and blockers, and explicitly denies default live execution.
- Phase 276 adds read-only marketplace lifecycle default UX command panel digest clipboard review packets over Phase 275 clipboard packets; the review remains operator/reviewer-only, redacted, filterable by section/state/cap, maps clipboard items to bounded ready-command and approval-remediation review items plus fixed safety summaries and review text, preserves copy text and blockers, and explicitly denies default live execution.
- Phase 277 adds read-only marketplace lifecycle default UX command panel digest clipboard review closeout packets over Phase 276 review packets; the closeout remains operator/reviewer-only, redacted, filterable by section/state/cap, maps review items to bounded ready-command and approval-remediation closeout items plus fixed safety summaries and closeout text, preserves copy text, review lines, and blockers, and explicitly denies default live execution.
- Phase 278 adds read-only marketplace lifecycle default UX command panel digest clipboard review closeout summary packets over Phase 277 closeout packets; the summary remains operator/reviewer-only, redacted, filterable by section/state/cap, maps closeout items to bounded ready-command and approval-remediation summary items plus fixed safety summaries and summary text, preserves copy text, closeout lines, review lines, and blockers, and explicitly denies default live execution.
- Phase 379 through Phase 381 add operator-surface trust hardening for provider performance/failover drilldowns, doctor/onboarding diagnostics, and memory status/routing/palace inspection. These are redaction and truth-surface closures only; they do not add default live plugin install, public hosting, default external channel delivery, credential persistence, push, or PR creation.
- AGENTS, roadmap, gap analysis, references, help text, and runtime surfaces all describe the same current system.

## Phase Exit Bar

Capability class is considered at parity only when:

1. the subsystem is implemented
2. verification exists
3. operator surfaces describe it truthfully
4. security and approval rules are enforced end-to-end
5. performance and compatibility are acceptable for the claimed scope
